# 优化建议与代码审查

> 审查范围：`src` 下全部 TypeScript/Vue 文件、配置、测试、文件系统操作、错误处理、依赖和文档。  


## 一、总体结论

当前项目功能较完整，暂未发现需要阻断发布的高风险问题。主要维护风险来自职责集中、错误边界不统一，以及大型模块持续膨胀。

建议评级：**中等风险，适合持续重构，不需要推倒重来。**

近期优先级：

1. 统一保护 `localStorage` 和 IndexedDB 访问。
2. 修复 `openCleanupDialog()` 的未捕获异常。
3. 将 `App.vue` 中的业务状态逐步下沉到 composable。
4. 按数据边界拆分两个大型文件系统模块。
5. 补充异常存储环境、回滚失败等测试。
6. 让 `package.json` 版本号与实际 Release 保持一致。

## 二、架构与维护建议

### 1. 建立组件边界

组件负责结构和行为，CSS 负责布局。共享组件不要承担具体弹窗的定位：

```text
CloseButton.vue  -> X 图标、ARIA、disabled、公共视觉
AboutDialog.vue  -> 右上角定位
Lightbox.vue     -> 标题栏内布局
```

只有模板、行为和视觉确实重复时才抽象，避免为了“看起来统一”强行拆分。

### 2. 已完成-按领域拆分 `styles.css` 

当前 [styles.css](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/styles.css) 已较大，建议逐步搬运，避免一次性产生大范围 diff：

```text
src/styles/
  tokens.css       # 颜色、间距、圆角、阴影、动画
  base.css         # body、button、滚动条、通用基础样式
  dialogs.css      # 通用弹窗、遮罩、关闭按钮
  top-bar.css
  album.css
  outfit.css
  lightbox.css
```

在 `main.ts` 中统一引入上述文件。

### 3. 建立通用 UI 组件层

建议目录：

```text
src/components/ui/
  CloseButton.vue
  DialogShell.vue
  IconButton.vue
  BaseButton.vue
  OperationNotice.vue
```

推荐顺序：`CloseButton`、`DialogShell`、`IconButton`，最后再抽 `ConfirmDialog` 的通用逻辑。

### 4. 按功能拆分状态

`App.vue` 当前约 2,073 行，同时处理相册、回收站、搭配码、X6Game 授权、专项清理、偏好设置、弹窗、本地存储和键盘焦点逻辑。建议逐步下沉：

```text
src/composables/
  useAlbumOperations.ts
  useTrashOperations.ts
  useOutfitOperations.ts
  useCleanupOperations.ts
  usePreferences.ts
  useDialogState.ts
```

优先迁移文件系统操作协调逻辑；不建议当前阶段引入 Pinia、UI 框架或复杂目录规范。

### 5. 完成-拆分国际化文案

当前 [i18n.ts](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/i18n.ts) 约 1,018 行。可保持 `messages[language]` 调用方式不变，仅调整内部组织：

```text
src/i18n/
  index.ts
  types.ts
  zh-CN.ts
  en-US.ts
  messages/
    about.ts
    topBar.ts
    lightbox.ts
    cleanup.ts
    outfit.ts
```

### 6. 分层测试

公共组件测试公共契约，业务弹窗只测试自身行为。例如 `CloseButton` 覆盖图标、ARIA、点击事件和 `disabled`；`AboutDialog`、`Lightbox`、`CleanupDialog` 分别覆盖各自关闭和业务流程。

## 三、代码审查问题

### 中风险

#### 1. 清理窗口打开流程存在未捕获异常

位置：[App.vue:1524](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/App.vue:1524)

`openCleanupDialog()` 在 `try` 外直接调用 `getSavedX6GameDirectoryHandle()`。IndexedDB 读取失败、浏览器拒绝访问或存储损坏时，会产生未处理 rejection。

建议将读取和授权恢复全部放入 `try/catch`；失败时保留窗口打开，并显示可理解的错误或提供重新授权入口。

#### 2. `localStorage` 初始化访问未统一保护

位置：[App.vue:111](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/App.vue:111)、[App.vue:186](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/App.vue:186)

多个初始化读取和 `ref` 默认值直接访问 `localStorage`。隐私模式、禁用站点存储或浏览器策略限制可能导致应用启动即抛异常。

建议统一提供 `readStorage(key, fallback)`、`writeStorage(key, value)`、`removeStorage(key)`，所有本地存储访问均通过封装完成。

#### 3. 核心页面职责过度集中

位置：[App.vue](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/App.vue)

相册加载、文件操作、搭配码、授权、清理、偏好和弹窗状态集中在单文件中，容易造成异步流程互相影响，测试也难以定位。按“相册、回收站、搭配码、清理、偏好、弹窗”边界逐步拆分即可。

#### 4. 文件系统模块职责过多

位置：[fileSystem.ts](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/utils/fileSystem.ts)、[outfitFileSystem.ts](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/utils/outfitFileSystem.ts)

两个文件均约 1,200 行以上，同时承担目录授权、IndexedDB、图片扫描、对象 URL、回收站、清理、ZIP 和搭配码元数据。建议按数据边界拆为 `storage`、`directoryAccess`、`photoFileSystem`、`trashFileSystem`、`cleanupFileSystem`、`outfitFileSystem`、`outfitBackup` 等模块。

#### 5. 专项清理递归统计可能造成内存和延迟峰值

位置：[fileSystem.ts:1092](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/utils/fileSystem.ts:1092)

`sumEntrySize()` 会在每层先收集全部子句柄，再递归读取文件大小。对于大型日志或缓存目录，可能产生明显延迟和内存峰值。

建议增加条目数或扫描时间限制，分批处理，显示“正在统计”状态并支持取消；必要时加入短生命周期统计缓存。

#### 6. 删除回滚失败时提示不足

位置：[outfitFileSystem.ts:777](C:/Users/GYKJ_Robot_001/Desktop/Infinity-Nikki-Album-Manager/src/utils/outfitFileSystem.ts:777)

回滚写入失败被静默吞掉，用户只能看到原始删除错误，无法判断回滚是否完整。

建议返回结构化结果，例如 `{ failedNames, rollbackFailedNames }`，并明确提示用户检查目录。

### 低风险

1. `package.json` 仍为 `1.0.0`，与已发布的 `v1.3.3` 不一致；建议统一版本来源并在发布流程中校验。
2. 部分批量导入和扫描只累计失败数量，建议保留有限的结构化失败原因，如 `invalid-metadata`、`read-failed`、`write-failed`、`invalid-image`、`permission-denied`。
3. `AboutDialog.vue`、`Lightbox.vue`、`OutfitEditor.vue` 的关闭、滚动锁定和焦点恢复逻辑重复，可抽 `CloseButton.vue` 与 `useDialog()`，但定位 CSS 仍由具体弹窗负责。
4. 测试仍缺少 `localStorage` 不可用、IndexedDB 失败、超大目录统计失败、删除成功但回滚失败、相册切换竞态，以及刷新/切换/关闭并发等异常环境场景。

已知测试问题：`AboutDialog.test.ts` 存在过期断言，当前结果为 `78 passed / 79 total`。建议下次版本更新时删除或改写该测试。

## 四、现有实现中的优点

- `tsconfig.json` 启用了 `strict`、`noUnusedLocals`、`noUnusedParameters` 和 `noFallthroughCasesInSwitch`。
- 文件系统批处理普遍设置并发上限；图片对象 URL 有 `revokeObjectURL` 释放逻辑。
- 图片导入限制大小、格式和像素数量；ZIP 导入校验条目数、路径和图片大小。
- 删除、写入和导入流程普遍考虑回滚。
- 测试覆盖并发读取、ZIP 流式导入、图片 URL 生命周期和主要弹窗行为。
- 当前 `npm run build` 已通过，`git diff --check` 未发现空白错误。

## 五、固定维护流程

每次新增功能建议按以下顺序执行：

```text
确认是否已有组件
确认是否已有 composable
确认是否已有样式 token
修改业务组件
补充对应测试
更新 log.md
npm test
npm run build
git diff --check
```
