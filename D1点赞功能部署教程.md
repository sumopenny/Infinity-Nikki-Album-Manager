# D1 点赞功能部署教程

按 Cloudflare 官方文档流程（[D1 入门](https://developers.cloudflare.com/d1/get-started/) · [Pages Functions 绑定 D1](https://developers.cloudflare.com/pages/functions/bindings/#d1-databases)）整理，适配本仓库现状。

> 本项目的一次性改动已就绪：`functions/api/like.ts`（接口）、`migrations/0001_create_like_counter.sql`（建表）、`wrangler.toml`（绑定声明）。你只需要完成下面第 2~5 步的**账号侧操作**。

---

## 0. 工作原理（30 秒版）

```
浏览器点击爱心 → POST /api/like
                  └─ Pages Functions（functions/api/like.ts）→ D1 表 like_counter 自增
网页打开时    → GET  /api/like → 返回 { count } 显示在按钮上
```

D1 不能从浏览器直连，必须经过 Pages Functions，所以部署 = 「建库 → 建表 → 把库绑到 Pages 项目」三件事。

## 1. 登录 Wrangler（只在迁移或部署时需要）

本地远程数据库启动不需要 Wrangler。只有执行远程迁移、查询 D1 或使用 Wrangler 部署时才需要它；项目继续使用 `npx` 临时下载，不把 Wrangler 放进网站运行依赖。首次执行远程命令前先登录：

```bash
npx wrangler@latest login
```

会打开浏览器让你授权 Cloudflare 账号。（第一次运行会下载 wrangler，稍等片刻。）

## 2. 创建 D1 数据库（一次，✅ 已完成）

```bash
npx wrangler d1 create wxnn-likes
```

输出类似：

```
✅ Successfully created DB 'wxnn-likes'
database_name = "wxnn-likes"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

**把这串 `database_id` 复制下来**，填进 `wrangler.toml` 里替换 `PASTE_DATABASE_ID_HERE`。（本项目已填：库 `wxnn-likes`，id `1fc2cb15-3458-4035-acdd-2a2caa203aa4`。账号里另有一个 `nikki` 库，与本功能无关。）

## 3. 执行远端迁移建表（✅ 已完成）

```bash
# 远端 D1（生产数据库）：
npm run d1:migrate:remote
```

这条命令会把 `migrations/` 里的 SQL 应用到远端数据库（建立 `like_counter` 表并插入初始行 count=0）。本方案不使用本地 D1 副本。

## 4. 把数据库绑定到 Pages 项目（控制台操作）

有两条路，选一条：

### 方式 A：控制台绑定（推荐，和现有「git push 自动部署」流程一致）

1. 打开 [Cloudflare Dashboard](https://dash.cloudflare.com/) → **Workers 和 Pages** → 选中你的 Pages 项目（无限暖暖相册那个）；
2. 进入 **设置（Settings）→ 函数（Functions）**；
3. 找到 **D1 数据库绑定**，点 **添加绑定**：
   - 变量名称（Variable name）：**`DB`**（必须和 `functions/api/like.ts` 里的 `env.DB` 一致）；
   - D1 数据库：选择 `wxnn-likes`；
4. 分别在 **生产（Production）** 和 **预览（Preview）** 环境里添加（预览可选）；
5. 保存。

### 方式 B：直接用 wrangler.toml 声明

`wrangler.toml` 里的 `[[d1_databases]]` 已写好，填入第 2 步的 `database_id` 后一并提交即可。注意：**Pages 项目以控制台的绑定配置为准**，toml 里的绑定只在用 `wrangler pages deploy` 部署时生效；你走 git push 自动构建的话，请以方式 A 为准。

## 5. 部署触发

绑定配置好后，**需要触发一次新部署才生效**。直接 push 一个 commit，或在控制台 Project → Deployments → 最新一条 → Retry deployment。

验证：

```bash
curl https://你的域名/api/like        # 应返回 {"count":0}
curl -X POST https://你的域名/api/like # 应返回 {"count":1}
```

网页顶栏搜索框左侧出现 ❤ 计数，点击 +1，刷新后数字保留（存在 D1 里）。

## 本地网站访问远端 D1

本地网站不能直接连接 D1，使用 Vite 代理把 `/api` 请求转发给已经部署的 Pages Function。双击下面的脚本即可启动：

```text
start\Start-Remote-D1-Website.bat
```

脚本默认代理到 `https://infinity-nikki-album-manager.pages.dev`，也可以把其他 Pages 地址作为第一个参数传入。远程模式会保留现有点赞按钮，因此本地点击点赞会真实写入线上 D1。

手动启动时使用：

```bash
set REMOTE_API_ORIGIN=https://infinity-nikki-album-manager.pages.dev
npm run dev:remote
```

普通 `npm run dev` 不会连接远程数据库。项目仍保留 `dev:pages` 和 `d1:migrate:local` 命令供将来离线调试使用，但本方案不会调用它们。

## 常见问题

| 问题 | 原因与处理 |
| --- | --- |
| 打开网站没有爱心按钮 | `/api/like` 不可达 → 检查 Pages 是否配置了 D1 绑定、绑定名是否叫 `DB` |
| API 返回 500 `no such table` | 忘了执行迁移：`npm run d1:migrate:remote` |
| 本地页面没有爱心按钮 | 确认使用 `Start-Remote-D1-Website.bat` 或设置了 `REMOTE_API_ORIGIN`，普通 `npm run dev` 不会代理 `/api` |
| `wrangler.toml` 里的 id 还没填 | 只影响 Wrangler 迁移/部署命令；线上以 Pages 控制台绑定为准 |
| 想重置点赞数 | `npx wrangler d1 execute wxnn-likes --remote --command "UPDATE like_counter SET count = 0 WHERE id = 1"` |

## 费用提示

D1 有免费额度（每日一定量的读/写次数与存储），一个单行计数表几乎不会产生费用。即使没有绑定支付方式，超出免费额度后接口会报错但网站其他功能不受影响（点赞按钮降级隐藏）。
