## 2026-07-08

1.问题：下滑网页到页面底端时，左侧日期侧栏会因为 sticky 父容器边界往上跑，导致收藏夹按钮被顶部工具栏遮住一点。
- 状态：已解决
- 技术/方法：先前已经用 `ResizeObserver` 实时测量顶部工具栏高度，解决了顶栏高度变化造成的遮挡；这次继续排查发现页面滚动到底部时，`.sidebar-column` 的 `position: sticky` 仍会被 `.album-layout` 的底部边界限制，所以会出现侧栏被父容器带着往上走的视觉问题。处理时把桌面端左侧栏改成 `position: fixed`，用 `--sidebar-width` 统一侧栏宽度，并给 `.album-content` 增加同等左侧占位，避免内容压到固定侧栏下方；移动端仍保留原来的纵向布局，不强制固定。已执行 `npm run build` 验证通过，`styles.css` 诊断为空；`README.md` 已检查，本次只是布局样式修复，不影响使用说明，无需更新。
---

2.问题：一键启动脚本最后提示“可以关闭启动器窗口”，用户希望检查完成后启动器窗口自动关闭；随后又把 BAT/EXE 移入 `start` 文件夹，并要求改成通过 `相册网站启动器.lnk` 启动；最后要求 README 里把显示名称从 `相册网站启动器.lnk` 改成 `相册网站启动器`。
- 状态：已解决
- 技术/方法：先把 `Start-Project.bat` 末尾正常启动路径里的手动 `pause` 去掉，改成完成检查后提示 2 秒并自动结束窗口；报错路径里的 `pause` 保留，避免 Node.js、npm 或依赖安装失败时错误信息一闪而过。随后重新生成 `Start-Project.exe`，让它继续调用同目录下最新的 BAT 启动逻辑。本次因为 BAT/EXE 已移动到 `start` 文件夹，启动脚本不能再把自身目录当作项目根目录，所以改为从 `start` 目录向上一级定位项目根目录，再检查 `package.json`、安装依赖并启动 Vite 服务；同时确认快捷方式指向 `start\Start-Project.exe`。文档方面同步更新 `README.md` 的中英文启动说明、常见问题和项目结构，把推荐启动入口改为项目根目录的 `相册网站启动器.lnk`；后续又按用户习惯把 README 里的展示名称统一改为不带扩展名的 `相册网站启动器`，更贴近 Windows 默认看到的快捷方式名称。
---

## 2026-07-10

1.问题：增加“一键清理低画质与截图”功能，并在首次使用时先明确提示用户选择当前游戏的 `X6Game` 文件夹；授权保存后，后续点击需要直接清理，不再重复提示。
- 状态：已解决
- 技术/方法：浏览器无法从 `NikkiPhotos_HighQuality` 目录句柄直接取得 Windows 绝对路径或访问父目录，因此首次清理时让用户额外授权 `X6Game` 文件夹，再通过 `FileSystemDirectoryHandle.resolve()` 严格验证当前相册位于 `Saved\GamePlayPhotos\账号ID\NikkiPhotos_HighQuality`，从可信的 `X6Game` 句柄向下定位 `NikkiPhotos_LowQuality` 和 `ScreenShot`。授权句柄保存在 IndexedDB，已有且有效时直接复用；只有授权不存在、失效或与当前相册不匹配时，才先显示选择提示并重新打开目录选择器。清理过程仅逐个删除图片文件，保留目录和其他文件；README 已同步补充中英文使用说明。
---

2.问题：将相册状态卡片从右侧照片内容区移动到左侧收藏按钮下方。
- 状态：已解决
- 技术/方法：调整 `App.vue` 的模板层级，把 `.status-card` 移入 `.sidebar-column`，并放在 `.favorites-button` 后、日期侧栏前，使状态信息固定显示在收藏按钮正下方；保留原有响应式样式和数据绑定。README.md 已检查，本次仅调整界面布局，不影响使用说明，无需更新。
---

## 2026-07-11

1.问题：删除、清理等操作还在使用浏览器原生确认/提示窗口，用户希望单独做一个与网页风格一致、动画过渡流畅的自定义弹窗。
- 状态：已解决
- 技术/方法：新增 `ConfirmDialog.vue`，用 Vue 3 Composition API、`Teleport` 和 `Transition` 做居中自定义弹窗，样式接入现有主题变量，支持亮色/暗色主题、遮罩虚化、按钮悬停和进入/退出动画。把 `App.vue` 里删除照片、删除预览图、一键清理确认都改成统一的 Promise 式自定义确认弹窗；同时把首次授权 `X6Game` 文件夹前的 `window.alert` 改成弹窗回调，`fileSystem.ts` 只负责文件系统逻辑，不再直接调用浏览器弹窗。已检查并更新 README 相关说明，执行 `npm run build` 验证通过，诊断为空。
---

2.问题：选择“半尺寸 1:1”缩略图模式时，需要先用自定义弹窗提示该尺寸可能因页面图片加载太多导致卡顿，并让用户取消或确认更改。
- 状态：已解决
- 技术/方法：把 `changeThumbnailMode` 改成异步切换逻辑，仅在从其他尺寸切到 `half` 模式时调用现有 `ConfirmDialog`，弹出“选择该尺寸会因页面图片加载太多导致页面卡顿，确认更改吗？”提示；用户取消时保持当前尺寸，确认时才写入状态和 `localStorage`。同时补充中英文 i18n 文案，并更新 README 的缩略图说明。
---

3.问题：日期分组标题右侧的 `div.date-block-header > label` 看起来像普通勾选项，用户希望做成按钮形式。
- 状态：已解决
- 技术/方法：保留原有 `label + checkbox` 结构，避免破坏点击整块即可切换日期选择的交互；只调整 `.date-check` 样式，把它做成圆角胶囊按钮，隐藏原生 checkbox，增加未选中带边框空圆点、已选中勾选图标、悬停抬升、聚焦轮廓和已选中高亮状态，让日期选择按钮和页面整体按钮风格更统一。后续按反馈修正未选中状态，不再提前显示勾号，并给空圆点补充边框；选中状态背景也改为纯色，不再使用渐变。README.md 已检查，本次仅为视觉样式优化，不影响使用说明，无需更新。
---

4.问题：左侧日期侧边栏里的日期按钮，日期和图片数量上下排列，用户希望放在同一行显示。
- 状态：已解决
- 技术/方法：调整 `.date-link` 的 flex 布局，去掉纵向排列，改为横向居中并用 `justify-content: space-between` 分开日期和数量；同时给数量设置不换行，避免窄宽度下数字被挤到下一行。README.md 已检查，本次仅为视觉样式优化，不影响使用说明，无需更新。
---

5.问题：README 中文教程开头新增了一键清理入口和 `img/7.png` 截图，同时把缩略图展示改为两张不同宽度的图片，用户要求补齐对应英文部分。
- 状态：已解决
- 技术/方法：对照 README 中文教程和英文教程的功能列表，补上英文的一键清理锚点链接与 `img/7.png` 截图，并把英文缩略图说明下方同步改成 `img/5.png` 30% 宽度、`img/6.png` 70% 宽度的展示方式，保持中英文内容和截图结构一致；随后复查相关 README 片段确认已同步。
---

## 2026-07-14

1.问题：修复收藏视图全选可能包含不可见照片、收藏大图导航跳出筛选范围、失效的 X6Game 保存授权直接触发浏览器权限框，并清理项目自引用依赖。
- 状态：已解决
- 技术/方法：把全选、计数、批量删除和预览前后导航统一绑定到当前 `visiblePhotos`，确保收藏视图只操作收藏照片；恢复 X6Game 保存句柄时先查询权限，失效后先显示项目 `ConfirmDialog`，用户确认才请求浏览器重新授权；从 `PhotoItem` 移除未使用的 `file` 和 `fileHandle` 状态，并删除 `package.json` 中项目对自身的 `file:` 依赖。后续按用户反馈回退虚拟列表和按需对象 URL，照片恢复为读取时创建预览地址并一次渲染全部卡片；同时回退启动脚本的锁文件哈希、`npm ci` 和服务轮询，恢复原来的 `node_modules` 检查、`npm install` 与固定 4 秒等待。README 已同步恢复对应中英文说明。
---

2.问题：相册一次性读取全部高分辨率文件会导致页面卡顿，希望在不改变连续滚动和现有 CSS 布局的前提下改为懒加载。
- 状态：已解决
- 技术/方法：目录扫描阶段只解析文件名并保存 `FileSystemFileHandle`，不再立即调用 `getFile()` 或创建全部 Object URL；新增基于 `IntersectionObserver` 的独立图片组件，在照片接近视口约一屏时才提交加载任务，并用全局并发数 3 的队列等待图片完成解码，普通失败自动重试一次。卡片继续使用原有日期分组和网格 DOM，仅在图片框内部增加同尺寸骨架与失败占位，因此不会重现虚拟列表高度计算造成的 CSS 异常；大图预览可优先读取未加载原图，切换相册、清除路径、删除照片和卸载页面时会取消任务并回收对象地址。README 中英文浏览说明已同步更新，已执行 `npm run build` 验证通过。
---

3.问题：点击“半尺寸 1:1”缩略图选项时不再需要显示性能警告确认框，希望直接切换尺寸。
- 状态：已解决
- 技术/方法：删除 `changeThumbnailMode` 中仅针对 `half` 模式调用自定义确认弹窗的分支，将函数恢复为直接更新缩略图状态并写入 `localStorage`；同时移除该弹窗专用的中英文标题、提示内容和“确认更改”文案，其他删除、清理与授权弹窗不受影响。README 中英文功能说明已同步移除确认提示描述。
---

4.问题：README 内容过长且重复，希望突出重点、优化排版，在开头增加版本更新入口，并补充 Gitee Release 与更完整的 Node.js 安装说明。
- 状态：已解决
- 技术/方法：将原有 700 余行中英文教程重新整理为紧凑的双语 README，删除重复的下载、启动、功能和故障说明，统一为顶部导航、核心功能、快速开始、相册操作、安全提示、精简 FAQ 与开发命令；保留在线地址、本地启动器、相册路径、懒加载、收藏、删除和一键清理等必要信息。顶部同时新增 GitHub Releases 与 Gitee Releases 的版本更新和下载入口，并使用居中导航、截图、表格和提示块改善阅读层级；随后补充中英文 Node.js LTS 安装步骤，明确 Windows 安装包选择、PATH 选项、重开终端、`node -v`/`npm -v` 验证以及命令无法识别时的处理方式。
---

## 2026-07-21

1.问题：增加手动与聚焦自动刷新，并把左侧改为“全部照片、收藏夹、最近删除”三个独立入口；普通相册删除需要移动到当前相册的 `trash` 文件夹，最近删除支持容量统计、选择、大图预览、恢复和永久删除。
- 状态：已解决
- 技术/方法：把原先的收藏布尔筛选改为三态相册视图，增量刷新按文件名复用已有 `PhotoItem` 和对象地址，只增加新文件并释放外部已删除照片。文件删除采用先写入 `trash` 再移除源文件的顺序，并在失败时回滚；回收文件名持久记录删除时间、原文件名和收藏状态，恢复重名时使用 `_restored_N` 后缀避免覆盖。新增最近删除网格及大图恢复/永久删除操作，所有永久删除弹窗明确展示影响照片数和不可恢复提示；补充 Vitest 文件系统与组件测试，并完成桌面、移动端响应式检查。后续按反馈把 Album Status 状态框移到三个视图入口上方，将入口按钮和状态框统一为与拍摄日期框一致的 18px 圆角，并适当收窄桌面左侧栏；最近删除卡片也移除了文件名、单张大小和单卡恢复/永久删除按钮，只保留删除时间，顶部“恢复选中”按钮圆角统一为其他批量按钮的胶囊样式。README 中英文说明已同步更新，本次后续布局微调不改变使用方法，无需再次修改。
---

2.问题：大图预览切换照片时，“移到最近删除”按钮会因短暂进入禁用态而闪烁。
- 状态：已解决
- 技术/方法：确认闪烁来自 `isPreviewLoading` 切换原生 `disabled` 后命中全局透明度样式。改为在切图期间保持按钮外观不变，通过 `aria-disabled`、点击守卫和禁止指针事件拦截误操作；只有实际删除进行中才使用原生禁用态。新增大图切换回归测试，验证下一张仍在加载时按钮不改变透明度且不会触发删除。README.md 已检查，本次为交互反馈修复，无需更新。
---

3.问题：按整体优化方案重构相册管理器界面，要求保留现有刷新、懒加载、收藏和最近删除能力，同时改为白色占比更高、以分隔线组织的工作型界面，并补充三级时间轴、动态底栏、大图缩放和清理容量统计。
- 状态：已解决
- 技术/方法：引入 `lucide-vue-next` 并将顶部改为同一行品牌与 `Star / 刷新 / 当前相册 / 视图 / 更多` 操作，菜单使用类型化 props/emits、互斥展开、外部点击和 Esc 关闭，帮助内容从“更多”弹出分组弹窗。移除 Album Status 和页面分区卡片，左侧改为“相册”导航与默认仅展开最新年份、月份的三级时间轴；照片墙使用紧凑网格、悬浮信息层和按需收藏入口，选择后由统一底部操作栏提供普通相册与最近删除的批量动作。大图预览增加 50%–300% 固定步长缩放、滚轮和拖动，并保留切图防闪烁。清理流程在删除前只读取文件元数据，按实际成功项累计释放字节，读取或删除失败时保留文件并返回结构化原因。同步更新中英文 README 和中文函数注释，使用 Vitest 覆盖分组、清理失败、菜单、时间轴、底栏、悬浮信息和缩放交互，并在 1440×900、390×844 视口检查无横向溢出或遮挡；同时根据浏览器控制台结果修正多根顶栏组件 `$el` 不是真实元素的问题，改为测量 `.app-header` 后更新侧栏吸顶偏移。
---

4.问题：重构后的左右区域在不同电脑宽度下适配不足，顶栏透明度、Star 提示、标题渐变动画和菜单位置偏离原网页；收藏按钮取消后持续显示，半尺寸仍显示大小；日期滚动影响整个侧栏；浅色大图配色、圆角、尺寸及 125% 后缩放操作也需要修正。
- 状态：已解决
- 技术/方法：恢复原网页的 `--top-bg`、`blur(18px)`、`title-gradient-flow 4.8s` 和 260% 渐变背景，顶栏重新展示无跳转的 Star 提示及“刷新相册”“更多”文字；菜单默认在触发项下居中，并在窗口边缘按实际位置安全平移。相册布局恢复 `clamp()` 侧栏宽度和照片网格自适应断点，用贯穿布局高度的伪元素绘制竖线；侧栏本身禁止滚动，仅日期区域使用带稳定 gutter 的原样式滚动条，三个相册入口移除悬停阴影。全局小圆角统一提升到 22px，收藏外框缩小并降低透明度，点击后主动释放焦点解决取消收藏仍显示的问题，半尺寸元数据只返回时间。大图面板改回主题变量配色并放大到最高 1320px，图片、舞台和面板补齐一致圆角；拖动入口忽略缩放按钮，修复 125% 后按钮被指针捕获导致无法继续缩放。新增对应 Vitest 回归测试，并实测 1440×900、1024×768、390×844 均无横向溢出。
---

5.问题：进一步取消胶囊和圆形观感，要求固定使用 22px 圆角；相册侧栏与图片区需要占满超宽屏幕；上一轮对图片区卡片圆角等非缺陷视觉调整需要回退，并继续放大大图预览模块。
- 状态：已解决
- 技术/方法：将主要操作控件高度提高到 48px 并固定使用 22px 圆角，Star 提示移除胶囊背景；手机顶栏改为两列两行，完整保留“刷新相册、当前相册、视图、更多”文字。删除相册布局的 `max-width: 1800px`，改为全屏宽度和 8–20px 响应式安全边距，竖线位置同步跟随边距与侧栏宽度。精确撤销图片区上一轮的圆角覆盖，普通卡片/图片恢复 6px/5px，半尺寸恢复 12px/9px，日期选择和照片角标恢复 7px；收藏按钮仅保留尺寸、透明度和显隐缺陷修复。大图最大宽度从 1320px 提高到 1500px，减少视口留白并提高舞台高度。移动端菜单继续以按钮中心展开，边缘修正改用不含滚动条的 `clientWidth`，保证菜单完整处于可见区域。
---

6.问题：左侧相册入口、年份、月份和日期项目在上一轮圆角调整后垂直间距变大，影响时间轴浏览密度。
- 状态：已解决
- 技术/方法：保留全屏宽度和 22px 圆角，仅将相册入口的最小高度恢复为 38px，将年份、月份和日期项目恢复为 32px；`npm test`、`npm run build` 和 `git diff --check` 均通过。README.md 已检查，本次仅为侧栏间距调整，无需更新。
---

7.问题：尝试恢复重构前的左侧栏和页面背景配色后，用户要求撤回左侧栏配色修改，但继续保留旧版页面背景。
- 状态：已解决
- 技术/方法：撤销侧栏局部颜色变量、导航边框/底色、粉黄色时间轴状态等修改，使侧栏恢复为此前的透明结构与原有悬停/激活配色；保留亮色模式的粉色柔光白灰渐变和暗色模式的黑色渐变背景。`npm test`、`npm run build` 与 `git diff --check` 均通过。README.md 已检查，本次仅调整视觉配色，无需更新。
---

## 2026-07-22

1.问题：当前长会话即将结束，需要为完全没有上下文的新会话整理可直接接手的项目交接文档。
- 状态：已解决
- 技术/方法：新增根目录 `HANDOFF.md`，系统记录项目目标、工作区保护约束、刷新与最近删除等已完成功能、最近的侧栏/网格布局风险、CSS 技术债、品牌图标现状、后续人工验证计划、历史踩坑、关键文件导航以及当前 18 项测试和生产构建基线。同步检查 README，交接文档不改变产品使用方式，无需额外修改 README。
---

2.问题：前端重构遗留问题集中修复：确认弹窗打开时大图快捷键仍穿透生效；`styles.css` 残留大量重构前的死选择器；`i18n.ts` 存在未使用的中英文案键。
- 状态：已解决
- 技术/方法：大图快捷键穿透采用显式开关方案：`Lightbox.vue` 新增 `keyboardEnabled` prop，`App.vue` 在确认弹窗 `confirmDialog.visible` 为真时传入 `false`，一次性屏蔽 Esc、方向键和 Delete 穿透，并在 `Lightbox.test.ts` 补充上层弹窗禁用键盘的回归测试。`styles.css` 对照模板逐类 grep 验证后删除 40 余条零引用死规则（旧顶栏模块、缩略图下拉、状态卡片、旧大图导航/按钮、回收站摘要栏等）及 3 个无引用 CSS 变量，混合选择器仅移除死分支、保留存活部分，媒体查询同步清理；故意分层覆盖的规则（滚动条、存活类后置覆盖）保守保留。`i18n.ts` 删除接口与 zh/en 三处同步的 28 个死键（app 10、topBar 8、sidebar 1、lightbox 1、trash 8），全部先 grep 确认模板与测试零引用。验证：`npm test` 19/19 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为内部质量修复，不改变使用方式，无需更新。
---

3.问题：继续修复重构遗留的中等问题：一键清理英文翻译靠中文字符串精确匹配、大图打开时背景页面仍可滚动、收藏在切换相册或刷新后被裁剪丢失。
- 状态：已解决
- 技术/方法：清理失败原因改为结构化代码：`fileSystem.ts` 导出 `RelatedCleanupFailureReason`（`'unreadable-size' | 'remove-failed'`），`executeRelatedPhotoCleanup` 只返回原因代码，zh/en 的 `relatedCleanupStatus` 各自把代码映射为当前语言文案，彻底去掉英文文案对中文字符串的精确匹配；同步更新 `LocaleMessages` 接口与文件系统测试断言。大图滚动锁定：`Lightbox.vue` 在预览照片非空时设置 `document.body.style.overflow = 'hidden'`，关闭或组件卸载时恢复，并补充锁定/恢复回归测试。收藏丢失：移除 `replaceAlbum` 与 `applyRefreshResult` 中两处按当前相册照片裁剪 `favoriteIds` 的逻辑，收藏记录跨相册保留，本相册文件临时缺失后重新出现时收藏仍生效；`favoritePhotos` 仍按当前相册照片过滤，计数与显示不受影响。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为内部质量修复，不改变使用方式，无需更新。
---

4.问题：继续修复重构遗留的体验问题：大图预览同一张图被解码两次、双击打开预览时选态快速切换闪烁、顶栏“更多”菜单点击外链后菜单不关闭。
- 状态：已解决
- 技术/方法：双次解码：`photoLoader.ts` 的 `waitForImageLoad` 解码完成时返回原始宽高，抽出内部 `loadDecodedPhotoWithRetry` 并新增导出 `loadPhotoWithRetryAndSize`；`Lightbox.vue` 直接复用该解码结果设置宽高比，删除原先为测量尺寸额外创建的探测 `Image`，解码失败时仍打开预览面板由失败占位呈现。`Lightbox.test.ts` 的模块 mock 同步补充尺寸返回，移除不再需要的全局 `Image` stub。双击闪烁：`PhotoGrid.vue` 与 `RecentlyDeletedGrid.vue` 的单击处理器统一改为忽略 `event.detail > 1` 的第二击，双击只触发一次选态切换并正常打开预览，单击响应不引入延迟。外链菜单：`TopBar.vue` “更多”菜单的 GitHub、Gitee、小红书、抖音四个外链补充 `@click="closeMenus"`，点击后菜单随新标签打开同时关闭。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为交互细节修复，不改变使用方式，无需更新。
---

5.问题：`styles.css` 中 420px 超窄屏照片墙单列规则被文件后部同特异性的 1100px/520px 多列媒体查询静默覆盖，超窄屏实际显示两列，单列设计失效。
- 状态：已解决
- 技术/方法：把 `.photo-grid, .mode-half .photo-grid { grid-template-columns: 1fr }` 从文件前部的旧 420px 媒体块中移除，在 520px 媒体块之后新增独立的 420px 媒体块放置该规则，利用层叠顺序确保超窄屏单列最终生效，421px–520px 仍按重构设计显示两列；规则上方补充中文注释说明顺序约束。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为样式层叠修复，不改变使用方式，无需更新。
---

6.问题：用户希望大图预览改成顶栏信息 + 中央图片 + 底部居中工具栏的布局，左右切换按钮为半透明圆形，且只有鼠标移到图片左右两侧区域时才显示。
- 状态：已解决
- 技术/方法：`Lightbox.vue` 模板重排为三段：顶部 `.lightbox-header` 左侧放日期、时间、文件大小，右侧放关闭按钮；中部 `.lightbox-stage` 保留滚轮缩放与拖动，并新增左右两个 `.lightbox-nav-zone` 悬停区域，内部各放一枚半透明圆形 `.lightbox-nav-button` 切换按钮；底部 `.lightbox-footer` 只保留居中的 `.lightbox-toolbar`，缩放三键移入工具栏并与收藏/恢复、删除按钮用分隔线区分，上一张/下一张按钮从工具栏移到两侧悬停区域。样式上 `.lightbox-nav-zone` 为图片两侧各 22% 宽的绝对定位区域，按钮默认 `opacity: 0` 且不响应指针，仅在区域悬停或键盘聚焦（`:focus-within`）时淡入，保证键盘可用；520px 以下触屏设备没有稳定悬停，改为常显并缩小按钮与区域。没有上一张/下一张时对应按钮不渲染。测试依赖的 `.lightbox-zoom-controls`、`.lightbox-delete` 等类名全部保留。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为界面布局调整，操作方式说明不受影响，无需更新。
---

7.问题：大图预览底部工具栏按钮和右上角关闭“×”缺少可见的 hover 效果。
- 状态：已解决
- 技术/方法：原 hover 规则使用 `rgba(255, 255, 255, 0.13)` 白色半透明背景，浅色主题下面板为白色导致悬停几乎不可见。改为 `color-mix(in srgb, var(--text) 12%, transparent)` 按文字色混合，浅色/深色主题下都清晰可见，并新增按下态（更深背景 + `scale(0.94)` 轻微回弹）与 0.15s 过渡；按钮统一补 `cursor: pointer`，同时单独给禁用态补回 `cursor: not-allowed`，避免覆盖全局禁用样式。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为交互细节优化，无需更新。
---

8.问题：全站配色与 CSS 质量优化：浅色背景偏冷灰与暖粉控件不协调、主按钮浅粉配白字对比度不足、深色主题照片为发光描边、标题渐变色过杂、圆角与动效时长不统一、样式死代码与硬编码颜色残留。
- 状态：已解决
- 技术/方法：配色：浅色 `--page-bg` 改为暖白渐变（`#fffdf9 → #f7efe9`）；浅色主按钮文字改深玫瑰 `#7f3e44`（对比度达标）、hover 背景改 `#eab1b3`，深色 hover 背景改 `#8f6668` 保持白字可读；深色照片阴影由发光描边改为淡描边加方向投影，浅色照片阴影同步改为有方向的柔和投影；标题渐变收敛为粉→暖黄三色（深浅主题各一份），删除失效的 `text-shadow: var(--title-shadow)` 引用。体系化：新增 `--radius-sm/md/lg`（10/16/24）三档圆角并替换 10 处散值（卡片、相框、侧栏、通知、日期块、大图面板与舞台），`--ui-radius` 归入 16；新增 `--motion-fast/base`（0.15s/0.2s）替换 14 处交互过渡字面量，弹窗进出场动画保留原值。工程：`.primary-button` 三段分散定义合并为一段并删除重复 hover；顶栏 eyebrow 字号 9px 提为 10px；大图收藏激活色统一为 `var(--favorite-heart-fill)`、删除色统一为 `var(--notice-error)`、两侧切换按钮底色提取为 `--lightbox-nav-bg(-hover)`。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为视觉与样式质量优化，不改变使用方式，无需更新。
---

9.问题：右下角操作提示窗样式粗糙，需要更精致的设计并改为页面顶部居中提示。
- 状态：已解决
- 技术/方法：提示窗由右下角固定宽卡片改为顶部居中悬浮卡片（`top: clamp(14px, 3vh, 28px)` + `left: 50%` + `translateX(-50%)` 居中），宽度改为 `max-content` 自适应内容（上限 560px），圆角提升为 `--radius-lg`，垂直居中排列。去除左侧 4px 色条，改为整体淡色描边（强调色 26% 混合）+ 30px 图标 chip + 双层阴影（1px 近景线影 + 强调色 30% 微光大投影），背景 88% 透明叠加 `blur(18px) saturate(1.4)` 毛玻璃。进出场动画改为自上方 12px 淡入并 0.96 缩放，使用 `cubic-bezier(0.22, 1, 0.36, 1)` 回弹缓动；因位移改为水平居中，enter/leave 关键帧同步写入 `translate(-50%, ...)` 防止动画期跳动。移动端媒体查询同步改为顶部 10px 间距，文件末尾统一圆角规则移除 `.operation-notice`（其圆角由自身规则管理）。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为提示样式调整，操作方式不变，无需更新。
---

10.问题：刷新相册时顶部居中提示窗出现后会向左移动一点距离。
- 状态：已解决
- 技术/方法：初次修复将水平定位由 `left: 50%` 改为不受滚动条影响的 `left: 50vw`，复测后问题仍存在。确认根因是提示窗采用 `width: max-content`：加载中的短文案切换为恢复完成的长文案时，卡片自身变宽，中心点虽未移动但左边缘会向左扩展，视觉上表现为提示窗左移。现改为稳定响应式宽度 `width: min(480px, calc(100vw - 32px))`，加载前后尺寸保持一致；保留 `left: 50vw` 避免滚动条变化影响中心点。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为定位修复，无需更新。
---

11.问题：网页刷新后提示“已恢复 23 张照片，”末尾挂着多余逗号。
- 状态：已解决
- 技术/方法：`successStatus` 文案设计上要与 `successSuffix` 拼接（逗号为连接符），但页面加载时自动恢复相册的 `restoreSavedDirectory` 调用未传 `suffix`，导致逗号悬空。为该调用补 `suffix: 'continued'`，刷新后完整提示为“已恢复 N 张照片，已继续使用上次记住的相册文件夹。”，与手动重新授权路径的提示一致。验证：`npm test` 20/20 通过、`npm run build` 通过、`git diff --check` 无空白错误。README.md 已检查，本次为文案修复，无需更新。
---

12.问题：新重构版本准备发布，需要根据当前全部项目差异整理面向用户的发版内容说明。
- 状态：已解决
- 技术/方法：新增 `RELEASE_NOTES.md`，按全新界面、增量刷新、最近删除、批量整理、大图预览、性能、清理反馈、兼容性与问题修复分类，将当前代码和日志中的技术改动转化为用户收益说明；单独强调普通删除、最近删除永久删除和一键清理的不同安全边界，并补充源码用户需重新安装 `lucide-vue-next` 依赖的升级提示。README.md 已检查，现有中英文功能与操作说明已覆盖核心更新点，无需为发布稿重复修改。
---

## 2026-07-24

1.问题：用户希望在现有基础上把界面 UI 再升级一档，让 CSS 细节更高级美观；体验后要求回退其中 6 处效果。
- 状态：已解决
- 技术/方法：只改 `styles.css`，不动任何组件结构。先在 `:root` 和深色主题里新增 `--ease-spring`（cubic-bezier(0.22,1,0.36,1) 回弹缓动）和深浅两套 `--primary-gradient(-hover)` 主色渐变变量，让全站动效和主按钮色系统一。最终保留的升级点：主按钮改为渐变底 + 顶部内高光 + 粉色光晕投影，hover 加深投影、active 时 `scale(0.97)` 回弹；顶栏 `backdrop-filter` 提为 `blur(18px) saturate(1.6)` 并用 `::after` 画一条主题色微光分界线；顶栏按钮补上过渡，hover 抬升 1px，按压缩小；下拉菜单改 90% 透明毛玻璃背景 + `blur(18px)`，加 0.18s 弹性入场动画（关键帧里保留居中的 `translateX(-50%)`，防止动画期跑偏）；日期侧栏链接加左侧 3px 主题色指示条，悬停时从 0 弹到 14px；选中徽章换用回弹缓动；大图两侧切换按钮隐藏态缩到 0.82，悬停时弹回 1；全局统一 `::selection` 主题色、按钮/链接 `:focus-visible` 焦点环和 `html` 平滑滚动。按用户要求回退的 6 处：顶栏按钮 hover 投影（保留抬升位移）、日期分组标题前的粉金渐变圆点、骨架屏渐变扫光（恢复呼吸脉冲）、收藏心形弹跳动画、照片悬停相框内 `scale(1.05)` 放大、底部选择栏胶囊形（恢复 16px 圆角与原阴影）。随后用户又要求恢复收藏心形弹跳动画，并新增心形按钮 hover 背景加深（未收藏态由 34% 深灰加深到 56%，已收藏态由 66% 玫瑰红加深到 86%），已重新加回。验证：`npm test` 20/20 通过、`npm run build` 通过。README.md 已检查，本次为纯视觉样式调整，不改变功能与使用方式，无需更新。
---

2.问题：README 仍把 Vercel 作为主要在线入口，并引导普通用户下载压缩包启动项目；现在国内可以通过 Cloudflare Pages 直连，需要调整使用说明。
- 状态：已解决
- 技术/方法：更新 `README.md` 的中英文首页链接、顶部提示和快速开始说明：普通用户优先访问完整可见链接 `https://infinity-nikki-album-manager.pages.dev/` 在线使用，`https://infinity-nikki-album-manager.vercel.app` 仅作为备选；顶部链接文本和快速开始说明都直接展示完整 URL，避免用户不知道哪里可以点击。按用户后续要求，把 Node.js 安装、Releases 下载、解压、启动器、本地运行、开发命令和技术栈说明统一移动到中英文介绍各自末尾的“开发者本地运行 / Local Setup for Developers”部分，避免普通用户在前面被本地部署内容干扰。文档校验：用关键字搜索确认已去掉“需要VPN/需要vpn”，开发者内容已位于普通使用说明之后；本次只改 README 文案，不涉及代码执行，未运行构建测试。
---

## 2026-07-31

1.问题：按照《无限暖暖方案》新增完整的搭配码管理功能，并接续异常中断会话中已经开始但尚未完成的修改。
- 状态：已解决
- 技术/方法：新增独立“搭配码”视图、标签侧栏、3:4 方案网格和新增/编辑弹窗；在当前相册下使用 `clothe` 文件夹保存标签、单方案 JSON 和 WebP 图片，支持待填写方案、外部图片自动接收、搭配码复制、标签筛选与删除、方案永久删除和大图预览。使用 `fflate` 实现带清单校验、路径安全检查、冲突处理和失败回滚的 ZIP 导入导出；搭配码缩略图比例独立记忆，并在打开相册、刷新、页面重新聚焦、关闭编辑器和导入后同步扫描。同步更新 README 中英文搭配码使用说明并补充文件系统、导航和组件测试。验证：`npm test -- --run` 28/28 通过、`npm run build` 通过；桌面与 390px 浏览器检查无横向溢出、控制台错误或空白画面。
---

2.问题：新建标签输入框显示在标签列表下方，标签删除叉号仅悬停时出现且悬停叉号会丢失整行背景；同时用户要求移除全站按钮渐变，并修正浅色模式下“添加方案”按钮 hover 文字没有保持白色的问题。
- 状态：已解决
- 技术/方法：将新建标签表单通过 `Teleport` 改为定位在加号右侧的独立浮层，监听窗口缩放和滚动更新位置；标签删除叉号改为常驻显示，并用父级行 hover 规则保证鼠标移到叉号时标签仍保持与拍摄日期一致的悬停背景，active 状态继续保留专用背景。后续按用户反馈改为给搭配码标签按钮自身增加与“拍摄日期-日”一致的左侧主题色指示条，悬停标签或悬停其删除叉号时都会展开；同时为选中的搭配码标签补充 active hover 规则，鼠标移到已选标签或其删除叉号上时仍保持黄色选中背景。全站主按钮和确认按钮的渐变背景替换为明暗主题纯色变量，保留页面背景、标题、图片遮罩等非按钮装饰渐变；为搭配码 Header 主按钮增加更具体的 hover 规则，避免后置的 Header 通用规则覆盖白色文字变量。README.md 已检查，本次主要为交互和视觉调整，无需新增使用说明。验证：自动化测试、生产构建与 `git diff --check` 通过；真实浏览器确认浮层位置、叉号与 hover 连贯性、所有按钮无渐变，并确认浅色模式“添加方案”悬停文字为白色。
---

3.问题：希望导出数据自动保存到当前相册文件夹，并在成功提示中提供打开导出文件所在文件夹的按钮。
- 状态：已解决
- 技术/方法：检查现有导出与提示组件后确认，ZIP 可以直接写入当前已授权相册目录，但纯浏览器环境受安全限制，无法直接唤起 Windows 资源管理器并定位该目录；曾尝试以系统文件夹选择窗口作为替代，但该窗口只适合选择文件夹且体验容易误导，后续按用户要求在第 15 项中移除“查看导出位置”按钮与功能，仅保留 ZIP 自动写入当前相册根目录和成功提示中的文件名说明。
---

4.问题：搭配码标签数量和字数上限偏低，标签标题区与“全部”筛选项过于紧凑；后续需要把搭配码说明从通用帮助迁移到进入模块时的独立指南。
- 状态：已解决
- 技术/方法：将用户标签上限统一由 18 个提高到 40 个、单标签长度由 4 个字符提高到 5 个字符，组件禁用状态、输入框长度、提交校验、导入合并限制和中英文提示均复用同一组常量；标签筛选列表增加 14px 顶部留白。先前放入“更多 → 帮助”的搭配码说明已撤回，新增独立搭配码指南弹窗，覆盖添加/编辑、图片直接放入 `clothe`、打开/刷新/重新聚焦自动接收、标签筛选、ZIP 导入导出和永久删除；进入模块时显示，勾选“不再提示”后用浏览器本地偏好关闭后续提示，搭配码标题旁的说明按钮可随时重新打开指南且不受该偏好影响。同步更新 README 中英文说明和 `无限暖暖方案.md`，新增指南组件、关闭偏好与原帮助内容撤回测试。验证：`npm test -- --run` 31/31 通过、`npm run build`、`git diff --check` 通过；真实浏览器验证首次显示、重复显示、勾选后隐藏、标题按钮重新打开，以及桌面/390px 指南布局无横向溢出。
---

5.问题：Edge 中主页面右侧显示浏览器原生根滚动条并占用页面宽度。
- 状态：已解决（根据反馈修正）
- 技术/方法：首次将需求误解为完全隐藏根滚动条；根据反馈恢复可见滚动条，并确认 Edge 可能由 `html` 而不是 `body` 承担页面根滚动。现为 `html` 和 `body` 同时应用 6px 细滚动条、透明轨道、圆角主题色滑块及悬停加深效果，并在根容器使用覆盖式滚动槽，避免另一层浏览器原生样式盖住定制样式和挤压页面内容；左侧日期/标签内部滚动条继续独立保留。README.md 已检查，本次为视觉布局修复，无需更新。验证：`npm test -- --run` 31/31 通过、`npm run build` 和 `git diff --check` 通过；Edge 实测根滚动条宽度为 6px、滑块使用主题色、页面滚动正常，桌面 `innerWidth` 与 `clientWidth` 均为 1440px，390px 窗口也无横向溢出。
---

6.问题：搭配码使用指南中的分区图标缺少粉色边框。
- 状态：已解决
- 技术/方法：为 `.outfit-guide-section-icon` 增加 1px `var(--pink-deep)` 主题色边框，使其在深浅色模式下与图标颜色保持一致。README.md 已检查，本次仅调整视觉样式，无需更新。
---

7.问题：搭配码使用指南底部“不再提示”复选框选中后的勾号需要显示为白色。
- 状态：已解决
- 技术/方法：将指南底部复选框改为自定义主题样式，未选中时显示粉色边框，选中时使用粉色背景和白色“√”，并保留清晰的键盘焦点轮廓。README.md 已检查，本次仅调整视觉样式，无需更新。
---

8.问题：调整搭配码指南的中英文操作说明，并让从标题说明按钮打开的指南同步显示已保存的“不再提示”勾选状态。
- 状态：已解决
- 技术/方法：按实际操作更新指南中英文文案，将第三部分标题明确为“批量导入搭配图片 / Bulk import outfit images”，说明添加方案时可选择、拖拽或在窗口空白处按 `Ctrl+V` 粘贴图片，搭配码可留空、单方案单标签、双击预览、标签数量与长度限制、删除标签的影响，以及通过 `clothe` 文件夹批量导入图片和自动转为待填写方案的时机。将“不再提示”从弹窗内部临时状态提升为 App 页面状态并作为 `dismissed` 属性传入指南：自动弹出和说明按钮打开共用同一偏好，关闭时根据当前勾选值写入或清除 localStorage。同步更新 README.md 中英文使用说明和《无限暖暖方案》，并新增已保存偏好重新打开时保持勾选的组件测试。
---

9.问题：搭配码在后台更新时没有进行中提示，用户无法判断扫描状态。
- 状态：已解决
- 技术/方法：为中英文搭配码消息新增“更新中”“已是最新状态”和带新增/失败数量的完成文案；新增统一的搭配码更新包装函数，开始扫描时显示带加载动画的顶部操作提示，结束后显示最新状态或扫描结果，异常继续进入统一错误提示。进入搭配码、关闭编辑窗口后的重扫、手动刷新以及页面重新聚焦触发的自动刷新均覆盖该状态；更新期间纳入全局忙碌状态，防止重复触发保存、导入或导出。同步更新 README.md 中英文说明和《无限暖暖方案》。
---

10.问题：搭配码使用指南窗口圆角与通用帮助不一致，编辑搭配码窗口及其内部控件圆角偏小。
- 状态：已解决
- 技术/方法：将 `.outfit-guide-panel` 与 `.help-dialog-panel` 的外框圆角统一为 20px；搭配码编辑窗口外框由 16px 提高到统一大圆角变量 `--radius-lg`（24px），编辑窗口内的关闭按钮、图片选择区域、替换图片提示、搭配码输入框和标签选项由 8px 提高到 12px，底部操作按钮原有胶囊圆角保持不变；浅色模式下图片选择区域默认背景单独改为纯白 `#fff`，深色模式与交互反馈不变。相关覆盖均限定在编辑窗口内，不影响标签浮层和方案卡片按钮。README.md 已检查，本次仅调整视觉样式，无需更新。
---

11.问题：编辑搭配方案时点击确定后窗口立即消失，用户无法在窗口中确认保存仍在进行。
- 状态：已解决
- 技术/方法：调整 `handleSaveOutfit` 的关闭时机，点击确定后编辑窗口继续显示并通过原有 busy 状态呈现“保存中”且禁用控件；只有文件写入与搭配码库重新扫描全部成功后才关闭窗口并清空编辑对象。若保存或扫描失败，编辑窗口保持打开并显示统一错误提示，用户可以检查内容后重试。后续反馈发现全局操作提示的 `z-index: 100` 低于编辑窗口的 `z-index: 115`，导致“保存中”被遮挡，现将操作提示层级提高到 140，使其稳定显示在编辑窗口、指南和确认弹窗上方。README.md 已检查，本次仅修正保存过程反馈，无需更新。
---

12.问题：浅色模式下大图预览舞台背景需要与搭配码图片选择区域一致改为纯白。
- 状态：已解决
- 技术/方法：为 `:root:not([data-theme='dark']) .lightbox-stage` 增加 `background: #fff` 覆盖，仅调整浅色模式；深色模式继续使用原有主题背景。README.md 已检查，本次仅调整视觉样式，无需更新。
---

13.问题：搭配码图片悬停时出现不需要的放大镜光标；搭配码大图底部缺少标签、搭配码和快捷操作，并要求从大图编辑后仍返回原预览。
- 状态：已解决
- 技术/方法：移除 `.outfit-card .photo-frame` 的 `cursor: zoom-in` 专用规则，保留双击打开预览行为但不再显示放大镜。扩展 `Lightbox` 的搭配码模式，接收当前 `OutfitItem` 与中英文搭配码消息，在底部工具栏显示标签和搭配码，并增加复制、编辑图标按钮；待填写方案的复制按钮保持禁用。App 通过当前预览 ID 派生最新搭配对象，编辑按钮只在大图上层打开 `OutfitEditor` 而不清空预览，保存或关闭时重新扫描后继续按 ID 绑定当前方案；编辑期间禁用底层大图键盘快捷键。编辑器同时改为记住并恢复打开前的 `body` 滚动状态，避免关闭上层编辑器后解除底层大图的滚动锁。移动端工具栏允许换行并限制信息宽度，避免横向溢出。同步更新 README.md 中英文说明和《无限暖暖方案》，新增大图搭配信息及复制、编辑事件测试。
---

14.问题：新建标签浮层打开后，点击页面空白处不会关闭。
- 状态：已解决
- 技术/方法：为加号按钮和通过 `Teleport` 挂载到 body 的标签表单分别保留元素引用，并在组件挂载期间监听文档级 `pointerdown`；点击目标同时位于按钮和浮层之外时调用现有 `closeTagInput` 清空输入并关闭浮层，内部输入、提交以及加号切换不受影响，组件卸载时同步移除监听。同步更新 README.md 中英文说明和《无限暖暖方案》，新增点击外部关闭浮层的组件测试。
---

15.问题：搭配码导出需要在操作前说明 ZIP 保存位置并自动写入当前相册；搭配码卡片需要像全部照片一样支持单击多选和底部工具栏，但不能收藏且删除不进入最近删除。
- 状态：已解决
- 技术/方法：导出按钮先使用现有确认弹窗显示当前相册文件夹名称，确认后将同一个 `FileSystemDirectoryHandle` 同时作为数据源和 ZIP 目标目录传给 `exportOutfitBackup`，取消原有自定义目标目录选择；按用户后续要求移除成功提示中的“查看导出位置”按钮与 `showDirectoryPicker` 快捷入口，操作提示组件也恢复为仅显示文本和关闭按钮。新增独立 `selectedOutfitIds` 状态及当前筛选的选中数、全选状态，搭配卡片单击/Enter 切换选中且内部复制、编辑、删除按钮阻止冒泡；`SelectionBar` 新增 outfit 模式，仅显示全选、永久删除和取消。批量删除前明确提示图片与 JSON 将永久删除且不会进入最近删除，逐项删除后汇总成功/失败并刷新列表。同步更新指南中英文文案、README.md、《无限暖暖方案》，新增卡片选择和搭配码底部栏测试。验证：38 项测试和生产构建通过；Edge 在 1440px 与 390px 视口下完成导出确认、ZIP 写入、多选工具栏和实际永久删除流程，无横向溢出或控制台错误。
---

## 2026-08-01

1.问题：用户希望搭配码可以从游戏目录自动更新：读取当前游戏 `X6Game\Saved\ShareCode` 最新 sharecode JSON，从文件名开头获取玩家 ID，再读取最新 `ShareCode`，并从 `X6Game\Saved\DIY\该玩家ID` 复制对应搭配图片到当前相册 `clothe` 自动写入 JSON；同时用户删除当前游戏最新自动导入项后，不应因游戏目录原文件仍存在而刷新复活。
- 状态：已解决
- 技术/方法：复用已有 `X6Game` 授权校验，不尝试越权从相册目录向上读取；相册路径解析只负责确认授权的 `X6Game` 与当前相册匹配，搭配码导入所用玩家 ID 从最新 sharecode 文件名开头提取，例如 `103203027diy_history_sharecode.json` 提取 `103203027`。新增 ShareCode 文件筛选、按修改时间取最新文件、JSON 数组倒序读取最新 `ShareCode`、重复搭配码跳过、按文件名 ID 进入 DIY 目录、最新图片转 WebP 写入 `clothe`、失败回滚与刷新结果计数。为删除后复活问题新增 `clothe/ignored-sharecodes.json` 临时忽略列表：删除成功后记录该搭配码；如果游戏最新仍是被忽略项则跳过；一旦检测到新的最新 `ShareCode`，自动清理旧忽略并导入新项。同步更新搭配码指南、README 和中英文授权文案，并补充内存文件系统测试覆盖最新导入、重复跳过、删除后忽略与新码清理。验证：`npm test -- --run` 45/45 通过，`npm run build` 通过；后续文案同步后再次执行最终验证。
---

2.问题：搭配码使用指南需要把“自动更新游戏搭配码”作为独立说明行，并更新自动更新、导入和导出的说明文案。
- 状态：已解决
- 技术/方法：将指南消息结构拆出 `guideFeaturedSection`，在弹窗介绍下方单独渲染自动更新游戏搭配码说明，下方四个卡片保留添加编辑、标签整理、批量导入、导入导出。按用户文案说明自动更新需授权 `X6Game`，并提示游戏内分享、点击截图右下角框选按钮、生成搭配码后回网页自动更新；导入导出说明同步改为导出前确认并写入当前相册 ZIP，导入校验合并且不覆盖已有方案，重复或无效内容跳过，删除搭配方案永久删除且不进最近删除。同步更新 README.md 和组件测试断言。验证：`npm test -- --run` 45/45 通过，`npm run build` 通过，`git diff --check` 无空白错误。
---

3.问题：用户希望“更多”菜单新增“清除缓存”和“清除数据”：清除缓存只保留当前相册授权，清除 `X6Game` 授权和搭配码指南“不再提示”等状态并重新加载当前相册；清除数据需要二次确认，清除所有网站本地记录和授权，让网站回到首次打开状态，且两个操作都不能删除电脑里的真实文件。
- 状态：已解决
- 技术/方法：在顶栏“更多”菜单新增两个操作入口，清除缓存时仅删除保存的 `X6Game` 目录句柄和指南勾选状态，保留当前相册目录句柄并用现有读取流程重新加载相册；清除数据时连续弹出两次确认，随后清除相册与 `X6Game` 授权、缩略图/主题/语言/收藏/指南等本地状态，并重置页面到无相册的初始状态。为清理流程增加本地持久化抑制开关，避免重置默认值时被 watcher 立刻写回 localStorage。同步补充中英文 i18n、TopBar 测试、README 功能概览和隐私安全说明。验证：`npm test -- --run` 45/45 通过，`npm run build` 通过。
---

4.问题：用户希望把双语 `README.md` 拆成中文和英文两个独立文件。
- 状态：已解决
- 技术/方法：保留根目录 `README.md` 作为简体中文说明，移除内嵌英文章节并在顶部加入 `README_EN.md` 入口；新增 `README_EN.md` 承载英文说明，并在顶部回链中文文档。拆分时保留原有项目链接、在线地址、截图、功能介绍、相册选择、常用操作、搭配码管理、最近删除、一键清理、隐私安全和开发者本地运行内容，避免改变实际使用说明。
---

5.问题：用户指出中文版 README 顶部在线地址、下载入口和“网站界面”都是重要文字，不应使用 Markdown 引用符号 `>`。
- 状态：已解决
- 技术/方法：仅修改中文版 `README.md`，移除第 15-26 行相关重要文字前的 `>`，保留原有文字、链接、截图和排版结构；英文版 `README_EN.md` 按用户要求未改动。
---

6.问题：用户希望把中文版 README 的“搭配码管理”功能说明放到“快速开始”后面。
- 状态：已解决
- 技术/方法：仅调整中文版 `README.md` 的章节顺序，将完整“搭配码管理”段落移动到“快速开始”说明之后、“选择相册”之前，保留段落原文和后续章节内容不变。
---

7.问题：用户希望中文版 README 快速开始里的“开发者本地运行”文字能跳转到同名章节。
- 状态：已解决
- 技术/方法：将第 50 行中的纯文本 `## 开发者本地运行` 改为 Markdown 内部锚点链接 `[开发者本地运行](#开发者本地运行)`，并顺手规范 `clone` 前后空格，方便点击跳转到本地部署教程。
---

8.问题：用户希望中文版 README 中搭配码大图截图居中显示。
- 状态：已解决
- 技术/方法：仅修改 `README.md` 中 `img/搭配码大图.webp` 这一处截图，将单独的 `img` 标签包进 `<div align="center">`，保留图片路径、alt 文案和 49% 宽度不变。
---

9.问题：用户希望中文版 README 中关于 `clothe` 文件夹管理搭配码的说明标红显示。
- 状态：已解决
- 技术/方法：仅修改 `README.md` 对应条目，使用内联 HTML 包裹该条说明，让该行在 README 渲染时以红色强调显示，其他搭配码说明不变。
---

10.问题：用户希望根据当前中文版 README 更新英文版 README。
- 状态：已解决
- 技术/方法：对照最新版 `README.md` 重写 `README_EN.md`，同步在线入口、WebP 截图、功能列表、快速开始内链、搭配码管理章节、红色/橙色强调、自动更新步骤图、一键清理、相册选择、常用操作、最近删除、FAQ、隐私安全、本地运行说明，以及 GitHub/Gitee Star 结尾链接。
---

11.问题：用户希望在英文版 README 开头增加遇到问题时通过 GitHub/Gitee Issues 或作者社交平台反馈的提示。
- 状态：已解决
- 技术/方法：在 `README_EN.md` 顶部居中介绍区的项目简介下方新增英文反馈提示，保留原有链接区和语言切换入口不变。
---

12.问题：用户希望中文版 README 开头也补充遇到问题时通过 GitHub/Gitee Issues 或作者社交平台反馈的提示。
- 状态：已解决
- 技术/方法：在 `README.md` 顶部居中介绍区的项目简介下方新增中文反馈提示，位置与英文版保持一致，原有 Releases、仓库链接和语言切换入口不变。
---

13.问题：用户要求再次根据当前中文版 `README.md` 更新英文版。
- 状态：已解决
- 技术/方法：按当前中文版内容重写 `README_EN.md`，同步顶部橙色反馈提示、在线入口、网站界面截图顺序与宽度、功能列表、快速开始、搭配码管理重点标色、自动更新步骤图、一键清理、相册选择、常用操作、最近删除、FAQ、隐私安全、本地运行和 GitHub/Gitee Star 结尾。
---

14.问题：用户希望在“当前相册”名称下拉框里增加“授权 X6Game”入口，方便手动提前授权游戏目录。
- 状态：已解决
- 技术/方法：在 `TopBar.vue` 的当前相册菜单中新增“授权 X6Game”菜单项，只在已选择相册且没有文件操作忙碌时可点击；在 `App.vue` 中新增手动授权函数，复用原有 `ensureSharedOutfitSource(true)` 和 `getX6GameDirectoryForAlbum` 校验流程，授权成功后显示完成提示，不新增额外文件访问路径。同步补充中英文菜单文案、授权成功提示、组件测试，以及中英文 README 功能说明。
---

15.问题：用户希望在搭配码界面取消 X6Game 文件夹授权窗口后，后续访问搭配码界面不再自动显示该窗口，并提示可在当前相册下拉框里手动授权。
- 状态：已解决
- 技术/方法：新增 `X6GAME_AUTO_PROMPT_DISMISSED_KEY` 记录自动授权弹窗取消状态，搭配码界面自动扫描会尊重该状态并跳过后续自动弹窗；取消时显示提示，说明可通过当前相册下拉框的“授权 X6Game”重新授权。手动授权入口改为绕过自动取消状态，授权成功后清除该记录；清除缓存和清除数据时也会重置该状态。同步补充中英文提示文案和组件测试。
---

## 2026-08-02

1.问题：用户希望在“更多”菜单的 Gitee 下方新增“打赏我”入口，点击后弹窗展示作者为爱发电的说明和微信、支付宝收款码，并在 README 末尾也展示收款码；随后用户觉得打赏窗口和内容偏小，要求放大，并希望“相册操作帮助”弹窗也使用同样的大小。
- 状态：已解决
- 技术/方法：在 `TopBar.vue` 更多菜单的 Gitee 链接下方新增“打赏我”菜单项，点击后打开打赏弹窗；弹窗复用帮助弹窗的遮罩结构，展示“作者为爱发电，网站制作不易，觉得好用的话可以来支持我~”说明和微信、支付宝收款码图片，图片用 Vite import 方式打包保证构建后路径正确，Esc 和点击遮罩可关闭。`styles.css` 新增打赏弹窗样式，收款码双列展示并自适应窄屏；后续按用户反馈将打赏面板宽度从 460px 放大到 640px，说明文字、间距和收款码图片（最大 260px）同步放大，并把 21px 弹窗标题提到两个弹窗共用的基础样式上，同时帮助弹窗宽度从 680px 放大到 760px，正文字号从 13px 放大到 15px、小节标题放大到 16px、分区内边距加大。`i18n.ts` 补充中英文打赏文案，`README.md` 和 `README_EN.md` 末尾追加打赏支持章节。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2.问题：用户希望把删除单个搭配方案的确认窗口文案从“确定永久删除“待填写”及其图片和 JSON 数据吗？此操作不会进入最近删除。”改为“确定永久删除此条搭配码吗？删除后不可恢复。”
- 状态：已解决
- 技术/方法：修改 `App.vue` 的 `removeOutfit` 函数确认框文案，中文改为新文案，英文同步改为对应含义，批量删除确认文案不在本次范围内保持不变；顺手为该函数补上中文注释。README.md 已检查，删除方案仍为永久删除，文档描述不受影响，无需更新。已运行 `npm test` 和 `npm run build` 验证通过。
---

3.问题：用户希望搭配码扫描提示按触发方式区分：用户主动点击“刷新相册”或进入“搭配码”界面时保留“搭配码更新中”和“已是最新”/结果提示；页面重新获得焦点、关闭编辑窗口自动重扫等后台扫描在没有新增和失败时完全静默；随后补充要求后台扫描在检测到新增或失败时也要弹出“更新中”状态和结果提示。
- 状态：已解决
- 技术/方法：梳理 `App.vue` 中所有扫描入口后，把 `refreshAlbum` 的“更新中”提示改为仅手动刷新时立即显示，“已是最新”也只在手动刷新时提示，后台刷新有新增/移除/失败时仍显示结果；`updateOutfitLibrary` 新增 `announce` 参数区分用户触发与后台扫描，关闭编辑窗口的自动重扫传 `false` 静默处理，进入搭配码界面保持原有完整提示。按补充要求，在 `outfitFileSystem.ts` 的 `readOutfitLibrary` 及相关导入函数中新增 `onImportStart` 回调，检测到待导入外部图片或游戏新搭配码时触发；`App.vue` 的后台刷新和后台重扫通过该回调补显示“更新中”状态（用标记避免重复弹出），扫描结束有新增或失败时再显示结果。保存、删除、导入等操作的结果提示不受影响。README.md 已检查，其中关于聚焦自动同步并提示新增/移除数量的描述仍然准确，无需更新。已运行 `npm test` 和 `npm run build` 验证通过。
---

4.问题：用户希望在“更多”菜单新增“关于网站”窗口，内容包括网站和功能简介、相册操作帮助里的鼠标操作和快捷键、网站更新日志（v1.2：新增星绘图册搭配码管理模块），并且打开网站时自动显示该窗口，带“不再提示”勾选框；本地记住版本号 1.2，版本号变化时重新弹出并清除勾选状态；随后要求窗口拓宽并取消滚动条，叉号不要自动出现焦点边框，当前版本日志要醒目，并补充“开发中”内容。
- 状态：已解决
- 技术/方法：`i18n.ts` 导出 `ABOUT_VERSION = '1.2'` 并新增中英文 about 文案（简介、功能列表、更新日志、不再提示等），更新日志做成数组结构便于后续追加版本；新建 `AboutDialog.vue` 组件，复用帮助弹窗的大窗口样式，内容含网站简介、主要功能、鼠标操作和快捷键（复用 topBar 帮助文案）、更新日志，底部为“不再提示”勾选框和确认按钮，支持 Esc 关闭和焦点循环；`TopBar.vue` 在打赏菜单项下方新增“关于网站”入口；`App.vue` 用 localStorage 记录 `{ version, dismissed }`，打开网站时若无记录、版本号与 `ABOUT_VERSION` 不一致或未勾选不再提示则自动弹出，关闭时保存当前版本号和勾选状态，版本不一致时勾选状态自动重置，清除全部数据会一并清除该记录。`styles.css` 新增关于弹窗样式，窄屏自适应；随后按反馈把窗口宽度从 760px 拓宽到 880px，取消面板自身滚动条让窗口随内容完整展开，遮罩层仅在屏幕过矮时兜底滚动；去掉打开弹窗时对关闭按钮的自动聚焦，解决叉号周围自动出现焦点边框的问题；当前版本日志条目旁新增“当前版本”徽章使其醒目，更新日志约定新版本排在数组开头；随后按用户要求把更新日志区块整体移到弹窗内容第一行，并给当前版本条目加高亮卡片样式（主题色描边、浅色底、加大版本徽章、加粗正文），打开窗口一眼可见；更新日志下方曾新增“开发中”区块，后按用户要求取消显示并移除相关文案；随后整体美化弹窗：区块标题加渐变竖条，当前版本日志升级为渐变高亮卡片，主要功能改为带渐变图标的三列悬浮卡片，鼠标/快捷键说明改为浅色信息卡片，面板圆角加大并加深主题色投影，窄屏自适应；再按用户反馈删除顶部渐变横幅（版本徽章、大标题、副标题），改为右上角悬浮圆形关闭按钮，删除日志条目旁的“当前版本”徽章并清理不再使用的 heroSub/currentVersion 文案，窗口宽度从 880px 拓宽到 980px 使内容不被遮挡。README.md 和 README_EN.md 的功能列表补充关于窗口说明并同步菜单描述。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
5.问题：用户要求删除“更多”菜单里的“帮助”入口及其“相册操作帮助”弹窗。
- 状态：已解决
- 技术/方法：`TopBar.vue` 删除帮助菜单项、帮助弹窗模板、`showHelp` 状态、CircleHelp 图标导入和 Esc 处理中的帮助分支；鼠标/快捷键帮助文案仍被“关于网站”窗口复用故保留，删除其余不再使用的 i18n 键（help、helpTitle、closeHelp、helpPathTitle、helpPathText、helpSafetyTitle、helpSafetyText 的中英文及类型定义）；`styles.css` 删除不再使用的 `.help-grid`、`.help-path` 样式及其窄屏媒体查询；`redesign.test.ts` 移除依赖帮助弹窗的断言。README_EN.md 的更多菜单描述移除 help 一项，README.md 对应描述为泛化表述无需改动。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
6.问题：用户反馈“关于网站”窗口会被“加载中”状态提示覆盖，要求关于窗口置于最顶层，确认窗口等弹窗次之，状态提示保持在这些窗口下方。
- 状态：已解决
- 技术/方法：调整 `styles.css` 层级：`.about-dialog` 设置 z-index 150 为全站最高（高于确认窗口 120、搭配码编辑 115、相册指南 121、标签弹层 130）；`.operation-notice` 状态提示从 140 降为 90，低于所有弹窗窗口（100 及以上）但仍高于大图预览（80），不再遮挡任何弹窗。已运行 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
7.问题：用户要求将其他窗口的 CSS 样式升级为与“关于网站”窗口一致的设计风格。
- 状态：已解决
- 技术/方法：在 `styles.css` 的 `:root` 新增 `--dialog-shadow` 统一双层主题色投影变量，关于、打赏、确认、相册指南、搭配码编辑五个窗口全部复用；各窗口圆角统一加大到 24px（确认窗口保持 26px）；所有窗口的关闭按钮统一为“关于网站”同款悬浮圆形样式（主题色浅底、悬停加深并放大 1.06 倍，搭配码编辑头部按钮因与小按钮组共用选择器而单独覆盖）；打赏窗口去掉头部分隔线、收款码改为浅色信息卡片并支持悬停上浮；标签编辑弹层投影改为主题色透明混合。均为纯样式调整，无模板和逻辑改动。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
8.问题：用户要求“搭配码使用指南”窗口拓宽、文字变大。
- 状态：已解决
- 技术/方法：调整 `styles.css` 中指南窗口样式：面板宽度从 760px 拓宽到 920px；头部角标 10→12px、标题 19→22px；引导文字 13→15px；分区标题 13→16px、正文 12→14px；底部“不再提示”文字 12→14px、勾选框 16→18px、确认按钮加高到 38px；各区域内边距同步加大（16/18px → 20/24px），分区图标 32→36px。已运行 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
9.问题：用户要求美化二次确认、警告类窗口（ConfirmDialog）。
- 状态：已解决
- 技术/方法：重写 `styles.css` 中确认窗口样式：面板加宽到 500px，投影在统一 `--dialog-shadow` 上叠加主题色光晕；随后按用户反馈取消顶部主题色渐变光带，恢复无装饰的简洁顶部；图标升级为 52px 渐变卡片并带主题色投影；角标、标题字号加大；底部按钮从右对齐胶囊改为全宽对称双列大按钮，主按钮改为主题色 135° 渐变 + 动态投影，次按钮带悬停描边加深；搭配码编辑器底部因复用 `confirm-dialog-button`，同步调整 footer 布局并为按钮设置 110px 最小宽度以保持右侧对齐。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2026-08-02
10.问题：用户反馈各弹窗的叉号关闭按钮 hover 后鼠标移走时背景色会闪一下。
- 状态：已解决
- 技术/方法：原因是关闭按钮 hover 时带有 `transform: scale(1.06)` 放大效果，鼠标移走时按钮缩小导致命中区域变化，在按钮边缘会反复触发 hover/取消 hover 造成背景色闪烁。修复方式为移除关于、打赏、确认、相册指南、搭配码编辑五个窗口关闭按钮 hover 时的 scale 放大，只保留背景色加深过渡。已运行 `npm run build`（类型检查与构建成功）验证。
---

2026-08-03
11.问题：用户要求在最近删除头部文字旁新增一键清空全部最近删除的按钮。
- 状态：已解决
- 技术/方法：在 `App.vue` 导入 Trash2 图标，最近删除标题 `<h2>` 改为 flex 布局，标题后追加红色色调的删除按钮（仅当有照片时显示，文件操作中禁用），点击弹出危险确认弹窗后调用已有的 `permanentlyDeleteTrashPhotos` 清空全部；`i18n.ts` 补充 `deleteAllTitle`/`confirmDeleteAll` 中英文文案及类型定义；`styles.css` 新增 `.trash-header-title` flex 容器与 `.trash-clear-all-btn` 按钮样式，悬停时变红色警示色调。已运行 `npm test`（45 个测试全部通过）和 `npm run build`（类型检查与构建成功）验证。
---

2026-08-04
12.问题：用户要求“搭配码使用指南”窗口再拓宽一点。
- 状态：已解决
- 技术/方法：调整 `styles.css` 中 `.outfit-guide-panel` 宽度，从 `min(920px, 100%)` 拓宽到 `min(1080px, 100%)`，其余样式不变。已运行 `npm run build`（类型检查与构建成功）验证。
---

## 2026-08-11

1.问题：用户要求一键清理低画质与截图不再强制当前相册名为 `NikkiPhotos_HighQuality`，只排除误选 `NikkiPhotos_LowQuality` 和 `ScreenShot`，同时保持账号路径校验；并调整 X6Game 授权窗口及搭配码指南文案。
- 状态：已解决
- 技术/方法：清理入口改为验证当前相册位于已授权 `X6Game\Saved\GamePlayPhotos\账号ID` 下，并仅拒绝文件夹名为 `NikkiPhotos_LowQuality` 或 `ScreenShot` 的选择，继续限制清理目标属于同一账号；授权标题改为“授权 X6Game 文件夹”，说明授权后可使用一键清理和自动读取最新搭配码，并提示选择启动器目录中的 `X6Game`；搭配码指南补充可从页面右上角当前相册下拉菜单授权。同步修正中英文 README 的清理选择条件和授权入口说明。
---

2.问题：用户要求按代码可读性、架构、性能和可维护性审查项目，并在不拆分 `App.vue`、`styles.css`、不新增公共文件系统工具和 CI 的约束下修复确定问题。
- 状态：已解决
- 技术/方法：整理 `App.vue` 脚本区块并把搭配码业务文案集中到 `outfitMessages.ts`；启动恢复在首次异步操作前获取加载锁并通过 `finally` 释放，避免与手动选择目录交错；卸载时补充释放搭配方案 Blob URL；删除完全被覆盖的日期侧栏滚动条规则；保留两个文件系统模块各自不同的目录查找语义。相册和搭配方案元数据扫描改为固定 6 路限流并发，ShareCode 与 DIY 候选遍历减少不必要集合与 I/O，并增加竞态、并发上限、排序、损坏回退和资源释放测试。
---

3.问题：用户反馈搭配方案批量导入图片明显变慢，要求将批量图片导入改为固定 3 路并发，随后说明实际导入的是包含 64 张 WebP 的搭配码 ZIP 备份。
- 状态：已解决
- 技术/方法：先为 `clothe` 中未配对散图增加固定 3 路 worker，预分配并保留唯一 ID，每张图片独立转换、写盘、验证和失败清理；进一步定位到 ZIP 备份走的是另一条串行路径后，将 ZIP 图片解码校验和方案落盘也改为固定 3 路并发。同搭配码或首选 ID 使用依赖链保持顺序，前项失败会释放占用让后项继续；64 张测试确认每张只解码一次，解码和写盘峰值并发均大于 1 且不超过 3。
---

4.问题：用户要求重点优化搭配码 ZIP 导入导出的处理速度，减少 WebP 重复压缩、主线程解压、串行写盘和导入后的重复全量扫描，同时保持旧备份、安全限制和失败回滚兼容；完成后发布 8.11 更新 v1.2.2，并预告家园码、组合码开发中。
- 状态：已解决
- 技术/方法：导出时 `manifest.json` 继续使用 Deflate，WebP 图片改用 `ZipPassThrough` 原样写入；导入旧 Deflate 备份时浏览器优先使用 `AsyncUnzipInflate`，Worker 不可用时回退同步解压，并增加异步 entry 完成屏障。导入前一次枚举目标文件名，在内存中分配 ID，成功后直接构造并返回完整 `OutfitLibraryResult`；App 统一应用返回结果，不再二次扫描、顺带导入散图或读取 X6Game。ShareCode 损坏回退改为单次目录遍历并排序。新增 store/Deflate 兼容、64 张并发、重复码失败释放、回滚、单次 ShareCode 遍历和 App 无二次扫描测试；最终 `npm test -- --run` 8 个测试文件共 68 项全部通过，`npm run build` 和 `git diff --check` 通过。随后将“关于网站”的版本号更新为 `1.2.2`，中英文当前版本日期改为 2026.8.11，更新内容为优化搭配码导入导出性能、完善 X6Game 授权与清理逻辑，并注明家园码、组合码功能开发中。
---

## 2026-08-12

1.问题：用户反馈搭配码新增标签总在列表底部，不便选择，要求新标签按新到旧显示，并允许在左侧标签栏拖拽调整顺序且同步到编辑器。
- 状态：已解决
- 技术/方法：将新增标签改为插入 `tags.json` 数组首位，数组顺序直接作为左侧栏与编辑器的共同展示顺序；左侧用户标签增加 Lucide `GripVertical` 六点手柄，使用 Pointer Events 统一支持鼠标与触屏，仅手柄启动排序，筛选按钮和删除按钮保持原行为。拖动时降低当前行透明度，并用主题色插入线标识目标位置；松手后乐观同步界面并调用现有 `saveOutfitTags` 持久化，写入失败则恢复原顺序。补充中英文无障碍文案、拖拽与禁用状态测试，并同步更新中英文 README。随后针对 Windows/Chromium 中原生 `title` 提示出现后 `grab` 光标偶发丢失黑色描边的问题，移除手柄原生 `title`，改为悬停或键盘聚焦时立即显示的 CSS 自绘提示，拖动期间隐藏提示，同时保留系统 `grab/grabbing` 光标和 `aria-label`。最终将“关于网站”的当前版本更新为 `v1.2.3`，日期更新为 2026.8.12，中英文更新日志说明新增标签置顶、拖拽排序以及编辑器顺序同步。验证：`npm test -- --run` 8 个测试文件共 72 项全部通过，`npm run build` 与 `git diff --check` 通过。
---

2.问题：用户反馈标签加号点击后浮层、搭配码编辑弹窗和编辑器内选择标签的动画不流畅，点一下就直接跳过去。
- 状态：已解决
- 技术/方法：排查发现三处根因都在 `styles.css`：搭配码编辑弹窗复用了 `confirm-dialog` 过渡名，但面板类名是 `outfit-editor-panel`，不在带 `translateY(18px) scale(0.96)` 的动画选择器里，导致只有遮罩淡入、面板瞬现瞬隐，把面板类名补进选择器后恢复了与其他弹窗一致的上浮缩放进出场；标签加号浮层的 `outfit-tag-editor` 过渡原本只在 active 里写了 `grid-template-columns` 过渡而起始态没有网格变化，实际只剩 0.2s 干巴巴的淡入淡出，改为从按钮侧 `translateX(-10px) scale(0.9)` 弹出、进入用带回弹的 `cubic-bezier(0.2, 0.9, 0.25, 1.12)`、离开快速收敛，侧栏和编辑器两处浮层同时生效；编辑器内标签选项按钮原本没有 transition，选中高亮瞬时切换，补上边框/背景/文字色 0.2s 过渡并加 `:active` 轻微回缩的按压反馈。README.md 已检查，本次为纯动效优化，不影响使用说明，无需更新。
---

3.问题：用户要求继续排查全站还有哪些地方的 CSS 动画流畅度可以优化。
- 状态：已解决
- 技术/方法：全面排查 `styles.css` 的过渡与各组件 `v-if` 硬切点后，保留了四处明确改进：大图预览原本 `v-if` 瞬现瞬隐，`Lightbox.vue` 根节点包上 `Transition name="lightbox"`，遮罩淡入淡出、面板 0.96 缩放、底部工具栏轻微上浮，leave 期间屏蔽指针防误触；大图前后切换图片时 `img` 补 `:key="displayedPhoto.id"`，新图 0.24s 淡入不再生硬闪切；菜单单选圆点 `.radio-mark` 选中时边框 1px→3px 加过渡平滑变粗；搭配卡片删除按钮 `.outfit-card-delete` 的 opacity 显隐加淡入过渡。另有三处改了之后用户要求回退（侧栏时间线/日期链接的透明描边方案、`.date-select-button`、`.outfit-filter-button` 的过渡），已恢复原样。验证：`npm test` 72/72 通过、`vue-tsc` 无错误。README.md 已检查，本次为纯动效优化，无需更新。
---

4.问题：用户指出全部照片多选后点击底部操作栏的收藏，此时收藏按钮应该变成取消收藏按钮。
- 状态：已解决
- 技术/方法：`SelectionBar.vue` 原本在相册模式下永远只显示“收藏”。新增 `allSelectedFavorited` prop，`App.vue` 用同名 computed 判断选中的可见照片是否全部已在 `favoriteIds` 中，全部已收藏时按钮切换为 HeartOff 图标的“取消收藏”并发出 `unfavorite`。同时调整 `unfavoriteSelectedPhotos`：原来无条件清空选择（那是收藏夹视图的设计，照片取消后会从列表消失），改为仅收藏夹视图清空，普通相册视图保留选择，按钮随之变回“收藏”可来回切换。`redesign.test.ts` 补充相册模式下按钮随收藏状态切换的测试。验证：`npm test` 73/73 通过、`vue-tsc` 无错误。README.md 已检查，底部操作栏“批量收藏”描述仍准确，按钮随状态切换属于交互细节，无需更新。
---

5.问题：代码评审指出“选中 ∩ 可见”的交集判断分散在多处，建议抽出统一的“当前作用域选择集合”计算逻辑，用户确认执行。
- 状态：已解决
- 技术/方法：`selectedIds` 跨视图保留，“选中的照片”和“当前看得见的照片”是两个集合，`visiblePhotos.value.filter((photo) => selectedIds.value.has(photo.id))` 这段交集代码散落在 `selectedCount`、`allSelectedFavorited`、`favoriteSelectedPhotos`、`unfavoriteSelectedPhotos`、`deleteSelectedPhotos` 五处。在 `App.vue` 新增 `scopedSelectedPhotos` computed 作为唯一定义点，五处全部改为从它派生，行为完全不变；以后新增筛选维度只需改这一处。验证：`npm test` 73/73 通过、`vue-tsc` 无错误。README.md 已检查，本次为内部重构，不影响使用，无需更新。
---

6.问题：用户反馈在选中照片状态下切换其他页面，选中状态还保持，认为不正常。
- 状态：已解决
- 技术/方法：定位到 `changeAlbumView` 之前只清空了搭配视图的 `selectedOutfitIds`，普通照片的 `selectedIds` 和回收站的 `trashSelectedIds` 没有清，导致切走再切回时选中残留。修复为视图真正变化时统一清空三个选中集合；同时加了 `isViewChanged` 判断，重复点击当前视图标签不会误清选中，避免手滑丢失选择，函数中文注释同步更新。验证：`npm test` 73/73 通过、`vue-tsc` 无错误。README.md 已检查，本次为交互行为修正，文档中的多选操作描述不受影响，无需更新。
---

7.问题：用户要求关于网站窗口再拓宽一点。
- 状态：已解决
- 技术/方法：把 `styles.css` 里 `.about-dialog-panel` 的宽度从 `min(980px, 100%)` 提到 `min(1080px, 100%)`，与指南窗口当前宽度一致；此前已按同样方式从 760px 逐步拓宽到 980px，本次继续沿用这个思路。验证：`npm run build` 通过。README.md 已检查，本次为界面尺寸调整，不影响使用说明，无需更新。
---

8.问题：用户要求关于网站窗口内的所有文字减小 1px。
- 状态：已解决
- 技术/方法：把 `styles.css` 关于窗口（`.about-dialog-*`）范围内所有显式字号统一减 1px：区块标题 16→15、正文 15→14（功能卡片和鼠标/快捷键说明的 p 都继承这一处）、版本标签 13→12、当前版本标签 15→14、底部“不再提示”文字 14→13、复选框对勾字符 13→12。底部确认按钮用的是全局 `.primary-button`（12px），不能直接改全局否则影响其他弹窗，所以在 `.about-dialog-footer .primary-button` 里加了 11px 的作用域覆盖并附中文注释。验证：`npm run build` 通过。README.md 已检查，本次为字号微调，不影响使用说明，无需更新。
---

9.问题：用户反馈英文界面的关于窗口模块和中文不一样，要求改成和中文界面一样。
- 状态：已解决
- 技术/方法：对比 `i18n.ts` 中英文案发现两处差异：英文 features 有 4 条（多了中文没有的“Recently deleted”，且搭配码管理排在第 3 位导致和图标顺序错位），中文是 3 条（相册管理/搭配码管理/一键清理）；英文更新日志文案只有前半句，缺了中文的动画优化说明、清理功能预告和大喵五花肉彩蛋。已把英文 features 删掉多余项并按中文顺序对齐为 3 条（AboutDialog 的 3 个图标按索引对应，数量一致后不会再有图标兜底错位），changelog 补全为中文文案的完整英文翻译。验证：`npm test` 73/73 通过、`vue-tsc` 无错误。README.md 已检查，本次为 i18n 文案对齐，不影响安装与使用说明，无需更新。
---

## 2026-08-15

1.问题：取消右上角相册下拉里的“一键清理低画质与截图”，改为在“视图”左侧新增“专项清理”按钮，点击打开独立清理窗口（参考小红书“快乐的00号”老师的清理项），窗口右上角提供授权文件夹按钮，标题下方提示需要授权 `X6Game` 文件夹。
- 状态：已解决
- 技术/方法：专项清理窗口（`CleanupDialog.vue`）包含 4 个清理项，每项标题+路径+描述、右侧“清理”按钮：低画质图片和截图（`ScreenShot` 与各账号 `NikkiPhotos_LowQuality`）、崩溃快照（`Saved\Crashes`）、运行日志（`Saved\Logs`）、内置浏览器缓存（`Saved\webcache_4430`）。授权改为独立于相册的 `pickStandaloneX6GameDirectory`（仅校验文件夹名为 `X6Game`），与搭配码自动更新共用 IndexedDB 句柄，一处授权两处可用；打开窗口时静默恢复已有授权。目录类清理采用“清空内容、保留文件夹”策略（`removeEntry(name, {recursive: true})` 逐条删除顶层条目），删除前弹确认框，完成后状态栏提示清理数量和释放容量。低画质项沿用“先从当前相册推导账号 ID”的原逻辑，推导失败时扫描 `GamePlayPhotos` 账号列表：单个账号直接使用，多个账号弹出 `CleanupAccountDialog`（可勾选全部账号或输入指定 ID，Promise 化）。期间踩了两个坑：一是 CleanupDialog 头部一开始用了 `<header>`，被 `.help-dialog-panel > header button` 的圆形样式误伤授权按钮，改用 `<div class="cleanup-dialog-header">` 解决；二是 `entries()` 返回基础 `FileSystemHandle` 类型，递归统计大小时需加类型断言才过 `vue-tsc`。版本号升级到 1.3.0，关于窗口更新日志与功能介绍同步改为专项清理描述。验证：`npm test` 74/74 通过、`npm run build` 通过。README 中英文已同步重写“专项清理”章节并更新特性列表、注意事项和隐私说明。
---

2.问题：专项清理窗口打开时，顶部操作提示（清理结果、释放容量等）被窗口遮住看不到；且“清除数据”后窗口仍显示 X6Game 已授权。
- 状态：已解决
- 技术/方法：`.operation-notice` 原本刻意设为 z-index 90（低于所有弹窗，避免遮挡弹窗内容），但专项清理场景下清理结果提示需要在窗口上方可见，故提升为 160（高于最高层级的 about-dialog 150），注释同步更新。授权状态残留问题：`clearDirectory`、`clearCache`、`clearData` 三处都会清除 IndexedDB 里的 X6Game 句柄，但漏了重置新增的内存态 `cleanupX6GameHandle`，导致窗口仍按旧句柄显示已授权；已在三处统一补上 `cleanupX6GameHandle.value = null`（搭配码侧的 `sharedOutfitSource` 原本就由 `resetLoadedAlbumState` 重置，无需改动）。验证：`npm test` 74/74 通过、`npm run build` 通过。README 已检查，本次为缺陷修复，不影响使用说明，无需更新。
---

3.问题：多账号选择弹窗（CleanupAccountDialog）显示异常——标题和说明文字占住左侧，账号输入框和“清理全部账号”勾选项被挤到右侧一条窄列里，文字竖排换行。
- 状态：已解决
- 技术/方法：弹窗复用的 `.confirm-dialog-panel` 是 `grid-template-columns: auto minmax(0, 1fr) auto` 三列网格，原为“图标 + 内容 + 关闭按钮”设计；账号弹窗没有图标和关闭按钮，内容落进第一列（auto，随长文案撑到接近整宽），表单落进第二列后被压成窄条。修复：给 `.cleanup-account-panel` 覆盖为单列 `grid-template-columns: minmax(0, 1fr)`，内容、表单、按钮纵向排列（按钮区原有 `grid-column: 1 / -1` 在单列下不受影响），并补充注释说明覆盖原因。验证：`npm run build` 通过。README 已检查，本次为样式修复，不影响使用说明，无需更新。
---

4.问题：多账号选择弹窗里用 datalist 输入账号 id，选中一个账号后再点下拉箭头，列表里只剩当前匹配项，看不到其他账号，无法改选。
- 状态：已解决
- 技术/方法：这是原生 datalist 的固有过滤行为——输入值完全匹配某个选项后，下拉只显示匹配项。考虑到账号 id 本来就必须从检测到的文件夹列表中选（组件也校验输入必须在列表内），自由输入没有实际意义，直接把 input + datalist 换成原生 `<select>` 下拉选择器：任何时候点开都列出全部账号，首项为禁用的“请选择账号 id”占位。同步移除不再需要的无效输入提示（`showInvalidHint`、`accountInvalid` 文案），`accountInputPlaceholder` 改为 `accountSelectPlaceholder`（“请选择账号 id”），弹窗说明文案“请输入”改为“请选择”。样式上用 `appearance: none` 隐藏原生箭头，改用以 `--muted` 颜色绘制的自定义三角，保持与网页风格一致。验证：`npm test` 74/74 通过、`npm run build` 通过。README 已检查，本次为交互修复，不影响使用说明，无需更新。
---

5.问题：专项清理窗口里点击低画质项的“清理”后，顶部会先弹出“正在定位低画质照片和游戏截图...”的加载提示，用户认为这个窗口场景下不需要显示。
- 状态：已解决
- 技术/方法：删掉 `cleanLowQualityPhotos` 开头的 `preparingRelatedCleanup` 状态提示（该提示源自旧“一键清理”流程，当时需要先定位文件夹再弹确认框）；现在点击后直接进入账号选择或确认弹窗，无中间提示。同步移除 i18n 中不再使用的 `preparingRelatedCleanup` 文案（类型定义与中英文词条）。验证：`npm test` 74/74 通过、`npm run build` 通过。README 已检查，本次为提示精简，不影响使用说明，无需更新。
---

6.问题：多账号选择弹窗里，用户希望在“清理全部账号”右边新增“记住我的选择”功能；勾选后下次点清理窗口仍会打开并自动回填上次的选择，方便直接确认或改选其他账号。
- 状态：已解决
- 技术/方法：CleanupAccountDialog 新增 `rememberedChoice` prop 和 `rememberChoice` 勾选状态，confirm 事件参数扩展为 `(accountIds, remember, choice)`（choice 为 `'all'` 或具体账号 id）。App.vue 新增 localStorage 键 `infinity-nikki-cleanup-account-choice`（已加入 `WEBSITE_LOCAL_STORAGE_KEYS`，清除数据时会一并清除）：确认时勾选则保存选项、未勾选则清除；打开弹窗前读取保存值传入弹窗回填——`'all'` 回填为勾选“清理全部账号”，账号 id 回填为下拉选中项，同时自动勾选“记住我的选择”；保存的账号 id 已不存在时按无记忆处理。两个勾选项用 `.cleanup-account-options` 横向排列（flex-wrap，窄屏自动换行）。i18n 新增 `rememberChoice` 文案（记住我的选择 / Remember my choice）。验证：`npm test` 74/74 通过、`npm run build` 通过。README 中英文已同步更新多账号选择说明。
---

7.问题：用户觉得专项清理窗口文字偏小，要求参考“关于网站”调大字号，并把窗口拓宽一点。
- 状态：已解决
- 技术/方法：以“关于网站”的字号体系（正文 14px、区块标题 15px）为基准统一调大 `.cleanup-dialog-*`：清理项标题 14→15、描述 12→14（行高 1.5→1.6）、路径 11→12、授权提示和已授权标签 12→13、授权按钮和清理按钮 12→13（按钮最小高度 32→34、34→36，内边距同步加大）；面板宽度 680→760px、内边距 22→24px、清理项卡片内边距 14/16→16/18、列表间距 12→14。顺带把账号选择弹窗的“账号 id”标签 12→13 保持一致。验证：`npm run build` 通过。README 已检查，本次为界面字号与宽度调整，不影响使用说明，无需更新。
---

8.问题：专项清理窗口标题下方除了授权提示小字，还需要新增一行“一些清理项参考小红书 快乐的00号 老师”的来源说明。
- 状态：已解决
- 技术/方法：i18n 的 cleanup 段新增 `referenceHint` 文案（中文“一些清理项参考小红书‘快乐的00号’老师。”、英文 “Some cleanup items are inspired by Xiaohongshu creator ‘快乐的00号’.”），CleanupDialog 标题组在授权提示下方再渲染一行同款 `.cleanup-auth-hint` 小字，样式无需新增。验证：`npm test` 74/74 通过、`npm run build` 通过。README 已检查，本次为界面文案补充，不影响使用说明，无需更新。
---

## 2026-08-16

1.问题：用户希望在“更多”下拉菜单新增“问题反馈”入口，倾向通过问卷星嵌入或网站集成的方式收集反馈。
- 状态：已解决
- 技术/方法：先梳理了几种方案的利弊——问卷星/腾讯问卷外链最简单但会跳离网站；GitHub/Gitee Issues 对玩家门槛高；自建表单没有后端只能依赖不稳定的第三方表单服务或 mailto。用户确认选择“弹窗内 iframe 嵌入问卷 + 入口放在更多菜单”。实现上完全复用打赏弹窗的既有模式：`TopBar.vue` 更多菜单在“打赏我”下方新增带 Bug 图标的“问题反馈”菜单项，点击打开同款遮罩弹窗，内部 iframe 加载问卷页面；问卷地址集中在组件顶部的 `FEEDBACK_URL` 常量里，发布问卷后替换即可，目前是占位链接。考虑了两个细节：一是 iframe 懒加载，首次打开弹窗才设置 src，避免网站启动时就去请求第三方页面；二是弹窗底部放了一条“问卷加载失败？在浏览器中打开”的外链兜底，防止问卷平台限制 iframe 嵌入时用户无处可去。Esc 关闭逻辑并入现有 `handleDocumentInteraction` 的弹窗分支。`i18n.ts` 按既有结构补了 feedback/feedbackTitle/feedbackOpenExternal/feedbackClose 四个中英文案，`styles.css` 新增反馈弹窗样式（760px 高面板、iframe 占满内容区、窄屏占满视口）。README 中英文功能列表各补一行反馈入口说明。验证：`npm run build`（vue-tsc + vite）通过，`npm test` 74/74 通过。遗留事项：需要用户把 `TopBar.vue` 里的 `FEEDBACK_URL` 占位链接替换成实际发布的问卷地址。
---

2.问题：用户反馈嵌入的问卷页面顶部/底部有灰色条带，随后问红框内（问卷页面）的样式能不能优化。
- 状态：已解决
- 技术/方法：灰色条带是问卷星页面自身的背景（问卷内容在白色卡片里，卡片外的页面区域是主题灰色背景），iframe 跨域，本站 CSS 和 JS 都改不了它内部的任何样式，皮肤/背景色只能在问卷星后台调整，底部“隐私政策/问卷星技术支持/举报”是其免费版固定页脚（去除品牌是他们付费功能）。能优化的是我们自己控制的容器侧：去掉 `.feedback-iframe` 的 1px 边框（问卷页面和弹窗同为白色，边框造成了“双层框”观感，去掉后融为一体，保留用户已调的 24px 圆角）；弹窗高度从 760px 降到 640px，贴合单题问卷的实际内容高度，消除底部大段空白，以后题目变多 iframe 内部会自己滚动。验证：`npm run build` 通过。README 已检查，本次为样式微调，不影响使用说明，无需更新。
---

## 2026-08-18

1.问题：复查专项清理和问题反馈功能时发现问卷地址带尾部空格、反馈弹窗缺少加载失败状态、大目录容量扫描性能不足，以及文件大小读取失败时删除策略与文案不一致。
- 状态：已解决
- 技术/方法：移除问卷地址尾部空格，iframe 增加来源策略和加载失败提示，并补测试锁定 iframe 与外部链接使用同一正确地址。专项清理按产品规则继续尽力删除目标目录中的全部内容：文件大小读取失败不会阻止删除，而是在清理计划中用 `sizeKnown` 标记容量估算不完整，确认信息明确提示部分大小未知；目录递归统计复用项目现有的 6 并发任务池，避免逐文件串行等待，也避免无上限并发。账号选择记忆增加本地存储异常降级，存储不可用时仍可正常清理，只是不记住选择。X6Game 授权继续由专项清理和搭配码自动读取共用，不拆分授权记录。中英文 README 同步说明“大小未知仍尝试删除、删除失败才保留”的实际行为。
---

## 2026-08-24

2.问题：搭配码搜索不应匹配标签，搜索框聚焦时出现不符合主题的原生深蓝色叉号。
- 状态：已解决
- 技术/方法：将搭配码搜索字段收窄为备注和搭配码正文；搜索输入框隐藏 Chromium 原生搜索清除控件，仅保留页面自定义清除按钮，并用主题粉色边框和柔和外圈替换默认聚焦样式，避免两个叉号同时出现。

3.问题：搭配码图片悬停时出现重复编辑按钮，照片备注按钮与收藏按钮尺寸和颜色不统一，备注编辑使用浏览器原生提示窗口。
- 状态：已解决
- 技术/方法：删除搭配码图片上的重复悬停编辑按钮，保留卡片底部原有编辑入口；照片备注按钮改为与收藏按钮相同的 27px 方形、9px 圆角，并放到收藏按钮左侧，使用黄色图标强调。新增网页内备注编辑弹窗，提供主题化输入框、字数计数、取消/保存和 Esc 关闭，不再调用浏览器原生 prompt。

4.问题：顶部搜索框激活时的阴影与搭配码编辑输入框不一致。
- 状态：已解决
- 技术/方法：将 `.header-search input` 的聚焦阴影统一为 `.outfit-fields > input:focus` 使用的 `0 0 8px 1px` 主题粉色阴影。

5.问题：照片被选中后备注编辑按钮因 `focus-within` 持续显示，备注弹窗输入框阴影和搭配码编辑框不一致，底部确认按钮不明显。
- 状态：已解决
- 技术/方法：去掉卡片 `focus-within` 对备注按钮的显示条件，避免选中卡片后按钮无法消失；备注弹窗输入框改用与 `.outfit-fields > input:focus` 相同的阴影；为弹窗底部按钮补充明确的最小宽度、主题背景、文字颜色和间距，确保保存按钮始终可见。

6.问题：备注窗口关闭按钮需要与其他窗口保持一致。
- 状态：已解决
- 技术/方法：备注窗口关闭按钮改用现有 `confirm-dialog-close` 样式和 Lucide X 图标，移除备注窗口独有的叉号字体和尺寸规则。

7.问题：备注窗口关闭按钮实际仍与“关于网站”窗口的关闭按钮不同。
- 状态：已解决
- 技术/方法：确认参照窗口使用的是 `.about-dialog-close` 而不是 `.confirm-dialog-close`；备注弹窗改为与关于窗口相同的面板内绝对定位结构，统一 34px 圆形按钮、14px 顶部和右侧间距、背景及悬停效果。

8.问题：网页备注和搜索正常，但相册目录中找不到 `album-metadata.json`，保存流程没有验证文件系统最终结果。
- 状态：已解决
- 技术/方法：沿着目录句柄、备注保存和刷新读取流程排查后，发现元数据读取会把除文件不存在外的所有异常静默当成空数据，容易掩盖目录权限或句柄问题；现在仅对文件不存在降级为空元数据，其他读取错误直接提示。写入关闭后立即回读并校验 JSON 版本和照片映射结构，未真正落盘时不再误报保存成功。

1.问题：用户需要为照片和搭配码添加不超过 15 个字符的备注，并在网页顶部搜索。
- 状态：已解决
- 技术/方法：照片备注集中保存到相册根目录的 `album-metadata.json`，打开或刷新相册时一次性读取，避免为每张照片单独读取文件；搭配码备注扩展现有 `clothe/<id>.json` 并兼容旧数据。顶部搜索按当前视图过滤照片文件名/备注，以及搭配码备注、搭配码和标签；照片卡片和搭配码卡片增加悬停编辑入口，照片大图支持编辑备注，搭配码复用现有编辑弹窗。同步中英文界面文案与 README，并移除顶部 Star 提示。

## 2026-08-24

1.问题：用户希望网页上方操作提示在鼠标悬停时不要自动消失，方便阅读较长的清理、导入和错误提示。
- 状态：已解决
- 技术/方法：OperationNotice 新增鼠标进入/离开事件，App.vue 将自动隐藏计时改为可暂停的剩余时间计时；鼠标进入通知时清除当前定时器并记录剩余时间，鼠标离开后从剩余时间继续计时，加载中的通知仍保持原有不自动消失行为。补充通知悬停事件测试。该改动只影响提示交互，不改变项目使用方式，因此未更新 README。
---

## 2026-08-21

1.问题：相册只接受以日期开头的文件名，其他图片即使扩展名受支持也不会显示。
- 状态：已解决
- 技术/方法：保留现有文件名日期优先逻辑，只有文件名完全无法匹配时才读取 `lastModified` 并生成分组、显示和排序所需时间。回收站也复用同一回退规则，保证这类图片删除后不会消失，仍可恢复；如果异常文件连时间都无法读取，则不纳入相册，避免产生不可靠的时间数据。中英文 FAQ 同步说明了两种时间来源。
---

2.问题：“关于网站”窗口需要保留当前版本，同时提供从 Git 历史整理出的正式版本更新记录入口。
- 状态：已解决
- 技术/方法：把关于窗口拆为关于页和历史版本页，当前版本页在“当前版本【2026.8.16更新】”后紧跟带历史图标的“更新记录”入口，历史页按版本号从新到旧展示 v1.3.0、v1.2.3、v1.2.2、v1.2.1 和 v1.2，不重复展示当前版本 v1.3.0 补充版；右上角关闭按钮在两个页面都有效，历史页返回按钮独占一行，标题在下一行显示，历史列表文字超出时在窗口内滚动且去掉每条记录前的竖线。更新文案从历史提交中关于窗口曾使用的 changelog 恢复，并同步中英文说明。
---

## 2026-08-25

1.问题：自动更新搭配码时，游戏生成新搭配码但 DIY 图片没有更新，网站仍会把旧图片绑定到新搭配码，造成重复图片卡片。
- 状态：已解决
- 技术/方法：调整导入顺序，先判断搭配码是否已存在；只有新搭配码才读取固定 DIY 图片，并将图片 `lastModified` 与历史自动导入记录中的 `diyImageModifiedAt` 比较。只有图片确实更新才写入新方案。自动导入元数据保存 `diyImageModifiedAt`，转换、写入或校验失败时统一清理半成品文件，并把重复搭配码、找不到图片、图片未更新、图片转换或写入失败区分为明确阶段提示。

## 2026-08-26

1.问题：需要将 tag 与 GitHub/Gitee Release 的发布经验沉淀为可复用的项目级技能。
- 状态：已解决
- 技术/方法：新增 `.agents/skills/release-tag-release/SKILL.md`，整理版本边界确认、中文 annotated tag、GitHub/Gitee 双远端同步、Release 标题和正文模板、Gitee 创建时间排序、令牌安全、删除重建确认和发布后验收规则；保留当前项目 7 个版本映射作为校验基准，不保存任何令牌或外部凭据。

2.问题：ZIP 导入会长期保留全部解压图片，JPG/PNG/WebP 保存后还会重复解码校验，导致大备份和批量图片导入占用较高内存与 CPU。
- 状态：已解决
- 技术/方法：保持备份格式 v1 不变，将 ZIP 导入改为两遍有界流式处理：第一遍只保留 `manifest.json` 并校验路径、条目数、展开大小、图片引用和格式，确认完整后第二遍最多并行处理 3 个图片任务，单图完成后释放解压字节；保留重复判断、同码顺序、标签合并、失败清理和最终提交回滚。图片保存改为源图只解码一次，尺寸和 4000 万像素上限在创建画布前检查，写入后仅核对文件大小，避免再次解码生成的 WebP。新增 WebP/PNG 单次解码、超大像素提前拒绝及 64 图并发上限测试。验证：`npm test -- --run src/utils/outfitFileSystem.test.ts` 24/24 通过，`npm run build` 通过。

3.问题：搭配方案导出 ZIP 后再导入，原有备注会丢失。
- 状态：已解决
- 技术/方法：排查发现备注字段在导出清单和导入元数据构造时都被遗漏。现在导出会把规范化后的 `note` 写入 v1 清单，导入会按现有 15 字符限制恢复到方案 JSON；旧备份没有该字段时按空备注兼容。补充真实“保存、导出、导入”往返测试，直接断言导入后的备注内容。

4.问题：批量移到最近删除、恢复和永久删除逐张串行，移动完成后还会重新扫描整个回收目录，照片较多时等待明显。
- 状态：已解决
- 技术/方法：按用户指定将图片复制和恢复任务池设为 6 并发，永久删除设为 10 并发，每张图片仍保持“目标写入成功后再删源、失败只回滚本张”的边界。移动函数直接返回可展示的回收项，页面按删除时间合并，不再为一次删除重新读取全部历史回收文件；首次加载或刷新回收站时复用 6 并发扫描。清空全部先核对 `trash` 内条目与页面目标完全一致，再递归删除目录；存在未知文件或目录状态变化时回退为 10 并发逐项删除。恢复名称增加并发占位，防止多个同名文件互相覆盖。

5.问题：搭配方案批量删除逐条串行，且每条重复读写忽略搭配码列表、删除后再完整扫描 `clothe`。
- 状态：已解决
- 技术/方法：新增批量删除核心，删除前最多 6 并发读取图片和 JSON 回滚备份，文件删除最多 10 并发；所有成功项完成后只读取并写回一次 `ignored-sharecodes.json`，提交失败时恢复本批已删文件。页面成功后直接移除内存中的方案并释放图片地址，只有事务失败才重新扫描。单个删除复用同一核心，保持原有失败恢复语义。

## 2026-08-28

1.问题：右上角主要操作的排列与期望不一致，需要调整为“当前相册、视图、刷新相册、专项清理、抽卡吉时、更多”。
- 状态：已解决
- 技术/方法：仅移动顶栏中现有按钮和菜单节点，不改变点击行为、禁用条件或菜单逻辑；新增组件测试，按 DOM 前后关系固定六个主要操作的顺序，避免后续调整时再次打乱。同步补充中英文 README 中此前遗漏的“抽卡吉时”娱乐性功能说明。

2.问题：关于网站的历史版本页仍沿用旧窗口尺寸下的固定列表高度，放大弹窗后文字区域与底部操作栏之间留下大块空白。
- 状态：已解决
- 技术/方法：让历史页占满正文剩余高度，并把历史列表改为可伸缩的滚动区域；移除桌面端和移动端旧的固定最大高度，使列表随弹窗尺寸自动延伸到底部操作栏上方。




