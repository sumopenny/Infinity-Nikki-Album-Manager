# Infinity Nikki Album Manager 交接文档

更新时间：2026-07-22  
工作区：`C:\Users\GYKJ_Robot_001\Desktop\Infinity-Nikki-Album-Manager`

## 一、任务目标

在保留原项目本地相册读取、懒加载、收藏、中英文和亮暗主题能力的基础上，把项目优化为适合大量照片整理的响应式桌面工具，并重点完成：

- 手动刷新与页面重新聚焦后的增量同步。
- “全部照片 / 收藏夹 / 最近删除”三个独立视图。
- 普通删除移动到当前相册的 `trash` 子目录，支持恢复与永久删除。
- 更紧凑、响应式、全屏宽度的顶部栏、左侧三级日期轴和照片网格。
- 动态底部批量操作栏和支持缩放、拖动的大图预览。
- 保留原网页的配色、标题渐变动画、顶部透明模糊效果及未特别要求改动的功能。

该项目仅使用浏览器 File System Access API，不增加后端、上传、遥测或云端依赖，目标浏览器为最新版 Chrome / Edge。

## 二、当前必须遵守的工作区约束

- 工作树是刻意保持的脏状态，所有已修改和未跟踪文件都属于当前需要保留的实现基线。
- 不要执行 `git reset --hard`、`git checkout --` 或批量覆盖现有改动。
- 修改前先查看 `git status --short` 和相关文件的实际差异。
- 文件编辑使用 `apply_patch`，避免 PowerShell 写文件导致编码或换行被整体改写。
- `log.md` 是 UTF-8 文件。PowerShell 读取中文时显式使用 `Get-Content -Encoding utf8`，否则终端会显示乱码，但不代表文件本身损坏。
- 新增或修改函数应保持项目现有的中文注释风格。
- 功能或使用方式改变时检查并同步中英文 `README.md`；每轮问题处理后按项目规范维护 `log.md`。

当前 `git status --short` 包含大量修改文件，以及以下未跟踪的新文件：

- `src/components/SelectionBar.vue`
- `src/components/redesign.test.ts`
- `src/utils/dateGrouping.test.ts`

## 三、已完成内容

### 1. 刷新与增量同步

- 顶栏增加“刷新相册”。
- 手动刷新重新扫描普通相册与 `trash`。
- 页面重新获得焦点时自动增量同步，并合并短时间内的 `focus` / `visibilitychange` 事件。
- 未变化照片复用原 `PhotoItem` 和已加载 Object URL，不重新加载缩略图。
- 新增文件进入懒加载队列；外部删除文件从界面和选择集合移除并释放 URL。
- 文件操作或确认弹窗期间不会触发自动刷新。

### 2. 最近删除

- 左侧入口顺序为：全部照片、收藏夹、最近删除。
- 普通相册的单张、批量和大图删除会移动到当前相册的固定 `trash` 子目录。
- 移动采用“写入 trash 成功后删除源文件”，失败时尽量回滚副本并保留源文件。
- 回收文件名持久化删除时间、原文件名、收藏状态和随机 ID。
- 最近删除按删除时间倒序展示，支持选择、全选、恢复、永久删除、永久清空和大图预览。
- 恢复时遇到重名使用 `_restored_N` 后缀，不覆盖现有文件，并恢复原收藏状态。
- 永久删除只处理 `trash` 中识别出的图片文件，允许批量部分成功并汇总失败。
- 最近删除无限期保留，不执行 30 天自动清理。
- 最近删除视图隐藏日期侧栏。

### 3. 页面整体重构

- 引入 `lucide-vue-next`。
- 顶栏包含品牌标题、Star 提示、刷新相册、当前相册、视图、更多。
- 当前相册菜单包含选择/切换目录、清除路径、一键清理低画质与截图。
- 视图菜单包含主题、缩略图比例和语言切换。
- 更多菜单包含帮助、GitHub、Gitee、作者，以及作者的小红书和抖音主页入口。
- 小红书链接：`https://xhslink.com/m/3IEU0XhZ6e`
- 抖音链接：`https://v.douyin.com/VdLd5oOXz8I/`
- 作者社交入口在同一行各占一半。Lucide 没有官方品牌图标，目前使用 `BookHeart` 和 `Music2` 作为语义图标，并保留平台文字。
- 未选择相册时显示中央引导、选择目录按钮和推荐路径。
- 页面主体改为全宽响应式布局，不再限制 `max-width: 1800px`。
- 左侧使用“年份 > 月份 > 日期”三级时间轴，默认展开最新年份和最新月份。
- 桌面端相册入口固定，只有日期区域滚动；移动端侧栏恢复普通文档流。
- 日期组标题采用“日期 · 数量”，照片网格间距更紧凑。
- 照片卡片常驻底部文字已移除；鼠标悬停或键盘聚焦时显示黑色渐变信息层。
- 半尺寸模式只显示拍摄时间，不显示文件大小。
- 收藏按钮悬停显示，已收藏时常驻；选中照片不会再强制显示收藏入口和时间信息层。

### 4. 批量操作栏

- 未选择照片时不显示；选择后从底部出现。
- 全部照片：全选、收藏、删除、取消选择。
- 收藏夹：全选、取消收藏、删除、取消选择。
- 最近删除：全选、恢复、永久删除/永久清空全部、取消选择。
- 批量收藏只添加尚未收藏的照片，不取消已有收藏。
- 收藏夹批量取消收藏后会同步清理已从当前视图消失的选择 ID。

### 5. 大图预览

- 支持上一张、下一张、收藏/恢复、删除/永久删除、关闭。
- 缩放范围为 50%–300%，步长 25%，支持按钮、滚轮和放大后拖动。
- 切换图片时重置为 100%，并保留原有删除按钮防闪烁处理。
- 修复了 125% 后缩放按钮因指针捕获而无法继续点击的问题。
- 修复了某些图片/视口组合下大图顶部出现空隙的问题：预览舞台现在使用原图宽高比，面板宽度计算也使用实际内边距与边框尺寸。

### 6. 清理与错误处理

- “一键清理低画质与截图”仍为永久删除，不进入最近删除。
- 清理前只读取文件元数据，不解码原图。
- 返回成功数量、释放字节数、结构化失败原因和缺失目录。
- 文件读取或删除失败时跳过该文件并保留原文件。

### 7. 样式与主题现状

- 页面粉色光晕位于右下角，明暗主题均已同步。
- 原标题动态渐变动画、顶栏 `--top-bg` 和 `blur(18px)` 已恢复。
- `src/styles.css` 原先存在两组重复 `:root`；现已合并为文件顶部一组浅色 `:root` 和一组暗色 `:root[data-theme='dark']`。
- 合并原则是重复变量优先保留旧 root 值，仅把新变量 `--sidebar-width` 和 `--ui-radius` 加入旧 root。
- 注意：虽然 root 已合并，但 `styles.css` 仍有很多旧选择器和文件后半段的重构覆盖选择器，后面的规则通常实际生效。

## 四、当前问题与风险

### 1. 需要优先人工复查的桌面布局

最近修复了左侧栏切换视图时轻微上移的问题：

- 桌面侧栏从 `position: sticky` 改为 `position: fixed`，避免主内容高度变化引起几像素位移。
- 固定定位导致侧栏脱离 Grid 后，照片区域一度错误进入第一列并与侧栏重叠。
- 已通过给 `.album-content` 增加 `grid-column: 2` 修复。
- `max-width: 900px` 下 `.sidebar-column` 仍由媒体查询覆盖为 `position: static`。

自动测试和构建通过，但该修复依赖真实浏览器布局，下一会话应首先在用户已授权的真实相册中检查：

- 1440×900、1920×1080 和更宽桌面屏幕。
- 全部照片、收藏夹、最近删除之间来回切换。
- 页面顶部、页面中段和接近底部时切换视图。
- 左侧栏不移动，照片区不与侧栏重叠，竖向分隔线贯穿页面。
- 390×844 或类似移动视口无横向溢出。

### 2. CSS 技术债

- `src/styles.css` 约 2800 行，包含重构前规则和后置覆盖规则。
- 同一选择器可能在前后多次出现，例如 `.sidebar-column`、`.album-content`、`.photo-card`、`.lightbox-stage`。
- 修改样式前必须使用 `rg -n` 查看所有声明，并以文件后部实际覆盖规则为准。
- 不建议在没有视觉回归检查时大规模删除旧规则。若要整理，应分模块合并并逐个视口验证。

### 3. 品牌图标

- 小红书和抖音目前不是官方品牌 SVG，而是 Lucide 的语义替代图标。
- 如果用户要求完全一致的品牌图标，下一步可使用本地 SVG 资源或轻量品牌图标包，但需注意版权、包体积和暗色模式可见性。

### 4. 文档与日志

- `README.md` 已包含刷新、最近删除、操作栏、大图缩放和安全说明。
- 最新的“收藏夹批量取消收藏”和作者社交主页入口尚未专门写入 README；可根据用户希望的文档粒度决定是否补充。
- `log.md` 当前可见最新日期标题为 `2026-07-21`，本会话 2026-07-22 的多轮微调尚未系统追加。后续继续工作前建议补一条汇总日志，而不是为每个细小 CSS 调整写重复记录。

### 5. 工作区未提交

- 整轮实现尚未提交 Git。
- 不要把脏工作树误认为无关改动。
- 提交前应先让用户确认最终视觉效果，再按功能拆分或整体提交。

## 五、下一步计划

1. 启动 `npm run dev`，使用真实相册授权复查最近的固定侧栏与主区域第二列修复。
2. 在桌面和移动端验证三个视图切换、日期滚动、底部操作栏和大图预览。
3. 验证收藏夹选中照片后“取消收藏”会立即移出当前视图，并且选择计数归零。
4. 验证更多菜单的小红书、抖音入口在新标签页打开，菜单在窄屏不溢出。
5. 若视觉无问题，补充 `2026-07-22` 的 `log.md` 汇总记录，并判断 README 是否需要加入社交入口和批量取消收藏说明。
6. 再次执行：

   ```powershell
   npm test
   npm run build
   git diff --check
   ```

7. 最后查看 `git diff --stat` 和关键文件差异，确认没有覆盖用户基线后再考虑提交。

## 六、踩过的坑

1. **固定侧栏会脱离 Grid 流。** 仅把 `.sidebar-column` 改为 `fixed` 会让后续 `.album-content` 自动占据第一列，必须显式设置 `grid-column: 2`，移动端则依赖 `display: block` 媒体查询。
2. **重复 root 会产生误判。** 以前修改顶部变量可能被文件后部 root 覆盖；现在 root 已合并，但普通选择器仍可能重复。
3. **PowerShell 中文显示乱码不等于文件损坏。** 使用 `Get-Content -Encoding utf8`，不要因为终端乱码重写整个文件。
4. **大图固定高度会产生留白。** 图片 `object-fit: contain` 配合固定舞台高度，在宽高比不一致时必然出现上下空隙；应让舞台使用图片真实比例，而不是用负边距遮盖。
5. **缩放拖动的指针捕获会吞按钮点击。** `pointerdown` 处理必须忽略按钮目标，否则放大后缩放工具会失效。
6. **切图加载态不要直接禁用并改变透明度。** 大图删除按钮曾因 `disabled` 样式在切图时闪烁；当前使用 `aria-disabled`、点击守卫和指针拦截保持外观稳定。
7. **选中状态和悬停状态要分离。** `.photo-card.selected` 不应强制显示时间渐变层或未收藏爱心；选中只保留边框和勾选标记。
8. **收藏夹需要独立操作栏模式。** 把收藏夹继续当 `album` 模式会只显示“收藏”，因此已新增 `SelectionBarMode = 'album' | 'favorites' | 'trash'`。
9. **文件系统移动不是原子操作。** 当前实现依赖“复制成功后删除源文件”，任何一步失败都要回滚并汇总失败项，不能先删除源文件。
10. **不要在增量刷新时重建所有照片对象。** 否则收藏、选择和已加载缩略图 URL 会丢失，且大量原图会重新加载。

## 七、关键文件导航

- `src/App.vue`：全局状态、视图切换、刷新、收藏、删除、恢复、批量操作和组件编排。
- `src/utils/fileSystem.ts`：目录扫描、刷新、trash 移动、恢复、永久删除和清理逻辑。
- `src/utils/dateGrouping.ts`：照片日期解析以及年/月/日三级分组。
- `src/components/TopBar.vue`：顶部菜单、帮助弹窗、项目与作者外链。
- `src/components/AlbumViewNav.vue`：全部照片、收藏夹、最近删除入口。
- `src/components/DateSidebar.vue`：三级日期轴及展开状态。
- `src/components/PhotoGrid.vue`：普通/收藏照片网格、悬浮信息和收藏按钮。
- `src/components/RecentlyDeletedGrid.vue`：最近删除网格。
- `src/components/SelectionBar.vue`：三种模式的动态批量操作栏。
- `src/components/Lightbox.vue`：大图加载、导航、缩放、拖动与删除/恢复操作。
- `src/styles.css`：完整主题与响应式布局，后置规则优先级尤其需要注意。
- `src/i18n.ts`：所有中英文文案和类型。
- `src/components/redesign.test.ts`、`albumViews.test.ts`、`Lightbox.test.ts`：主要组件回归测试。
- `src/utils/fileSystem.test.ts`、`dateGrouping.test.ts`：文件系统与分组测试。

## 八、当前验证基线

最后一次验证结果：

- `npm test`：5 个测试文件、18 项测试全部通过。
- `npm run build`：`vue-tsc --noEmit && vite build` 通过。
- `git diff --check`：通过，仅有预期的 LF/CRLF 提示。

交接文档本身创建后未改变运行时代码；若后续继续修改，以上结果需要重新执行确认。
